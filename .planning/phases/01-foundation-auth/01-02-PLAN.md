---
phase: 01-foundation-auth
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - lib/features/auth/domain/entities/user.dart
  - lib/features/auth/domain/repositories/auth_repository.dart
  - lib/features/auth/domain/usecases/login.dart
  - lib/features/auth/data/models/user_model.dart
  - lib/features/auth/data/datasources/auth_remote_datasource.dart
  - lib/features/auth/data/datasources/auth_local_datasource.dart
  - lib/features/auth/data/repositories/auth_repository_impl.dart
  - lib/features/auth/presentation/providers/auth_provider.dart
  - lib/features/auth/presentation/screens/login_screen.dart
  - lib/features/auth/presentation/widgets/password_field.dart
  - lib/core/services/auth_storage_service.dart
autonomous: true
user_setup:
  - service: Backend API
    why: "Authentication endpoints need to be accessible"
    env_vars:
      - name: API_BASE_URL
        source: "Backend deployment or local development server"
    notes: "For development, can use mock responses initially"
must_haves:
  truths:
    - "User can enter email and password on login screen"
    - "User can toggle password visibility with eye icon"
    - "Login button triggers authentication and shows loading state"
    - "Successful login stores JWT token and navigates to home"
    - "Failed login displays clear error message"
    - "User session persists after app restart (token retrieved from secure storage)"
  artifacts:
    - path: "lib/features/auth/presentation/screens/login_screen.dart"
      provides: "Login UI with email/password fields and visibility toggle"
      min_lines: 150
      contains: ["TextField", "IconButton", "CircularProgressIndicator", "Material 3 styling"]
    - path: "lib/features/auth/presentation/providers/auth_provider.dart"
      provides: "Riverpod state management for authentication"
      exports: ["authStateProvider", "loginProvider"]
    - path: "lib/features/auth/data/repositories/auth_repository_impl.dart"
      provides: "Repository implementing auth operations with secure storage"
      contains: ["SecureStorageService", "CacheService", "login", "getCurrentUser"]
    - path: "lib/core/services/auth_storage_service.dart"
      provides: "Auth-specific token management"
      contains: ["saveToken", "getToken", "clearTokens", "saveUser"]
  key_links:
    - from: "lib/features/auth/presentation/screens/login_screen.dart"
      to: "lib/features/auth/presentation/providers/auth_provider.dart"
      via: "ConsumerWidget with ref.watch/read"
      pattern: "ref\.watch\(authStateProvider\)"
    - from: "lib/features/auth/presentation/providers/auth_provider.dart"
      to: "lib/features/auth/domain/usecases/login.dart"
      via: "Use case invocation"
      pattern: "LoginUseCase\(" 
    - from: "lib/features/auth/domain/usecases/login.dart"
      to: "lib/features/auth/data/repositories/auth_repository_impl.dart"
      via: "Repository injection"
      pattern: "AuthRepository"
    - from: "lib/features/auth/data/repositories/auth_repository_impl.dart"
      to: "lib/core/services/auth_storage_service.dart"
      via: "Token persistence"
      pattern: "AuthStorageService"
---

<objective>
Implement complete authentication flow with login screen, password visibility toggle, secure token storage, and session persistence.

Purpose: Enable users to authenticate with email/password, see loading states and error messages, and remain logged in across app restarts. This is the core user-facing feature of Phase 1.

Output: Working login screen with full auth flow: UI -> Provider -> Use Case -> Repository -> Data Source -> Secure Storage.
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md

# Plan 01 Dependencies (must exist before this plan runs)
- lib/core/utils/result.dart (Result type)
- lib/core/errors/failures.dart (Failure classes)
- lib/core/errors/exceptions.dart (Exception classes)
- lib/core/di/injection.dart (Riverpod providers)
- lib/core/services/secure_storage_service.dart
- lib/core/services/cache_service.dart
- lib/main.dart (app entry point)

# Phase 1 Requirements Being Implemented
- AUTH-01: User can log in with email and password
- AUTH-02: User can view and toggle password visibility
- AUTH-03: User session persists across app restarts
- AUTH-07: App shows loading state during authentication
- AUTH-08: App shows clear error messages for failed login
- UI-01: App follows Material 3 design system
- UI-04: App shows loading states for async operations
- UI-05: App shows error states with retry options
- PERF-01: App launches in under 3 seconds (session check)

# Locked Decisions (from CONTEXT.md)
- Single-screen login with email and password fields visible together
- Eye icon toggle button for password visibility
- Session persistence: Indefinite (stay logged in until explicit logout)
- Password reset: Email link flow (not implemented in this phase - deferred)
- Material 3 design system throughout
- Loading indicators: Circular progress indicator (Claude's discretion)
- Error display: Snackbar with retry action (Claude's discretion)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Auth Domain Layer (Entities, Repository Interface, Use Case)</name>
  <files>
    - lib/features/auth/domain/entities/user.dart
    - lib/features/auth/domain/repositories/auth_repository.dart
    - lib/features/auth/domain/usecases/login.dart
  </files>
  <action>
    Implement Clean Architecture Domain layer for authentication:
    
    1. Create lib/features/auth/domain/entities/user.dart:
       - User entity with: id (String), email (String), name (String), avatarUrl (String?)
       - Use freezed for immutability: @freezed class User with _$User
       - Entity is the core business object (no Flutter dependencies)
    
    2. Create lib/features/auth/domain/repositories/auth_repository.dart:
       - Abstract class AuthRepository
       - Methods:
         - Future<Result<User, Failure>> login(String email, String password)
         - Future<Result<void, Failure>> logout()
         - Future<Result<User?, Failure>> getCurrentUser()
         - Future<Result<bool, Failure>> isAuthenticated()
       - Return Result type from core/utils/result.dart
    
    3. Create lib/features/auth/domain/usecases/login.dart:
       - LoginUseCase class
       - Constructor takes AuthRepository
       - Call method: Future<Result<User, Failure>> call(String email, String password)
       - Validation: Check email not empty and valid format, password not empty (min 6 chars)
       - Return Failure with specific message if validation fails
       - Delegates to repository.login() if validation passes
    
    Domain layer rules:
    - NO Flutter imports (pure Dart)
    - NO data layer imports
    - Entities are immutable
    - Repository interface defines contract
    - Use case encapsulates business logic
  </action>
  <verify>
    `flutter analyze` shows no errors in domain layer
    All domain files compile without Flutter dependencies
  </verify>
  <done>
    User entity exists with required fields, AuthRepository interface defined with all methods, LoginUseCase implements validation and delegates to repository
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Auth Data Layer (Models, Data Sources, Repository Implementation)</name>
  <files>
    - lib/features/auth/data/models/user_model.dart
    - lib/features/auth/data/datasources/auth_remote_datasource.dart
    - lib/features/auth/data/datasources/auth_local_datasource.dart
    - lib/features/auth/data/repositories/auth_repository_impl.dart
    - lib/core/services/auth_storage_service.dart
  </files>
  <action>
    Implement Clean Architecture Data layer:
    
    1. Create lib/features/auth/data/models/user_model.dart:
       - UserModel extends User entity
       - fromJson(Map<String, dynamic> json) factory constructor
       - toJson() method
       - Use json_serializable for code generation
       - Fields: id, email, name, avatarUrl, token (for API response)
    
    2. Create lib/features/auth/data/datasources/auth_remote_datasource.dart:
       - AuthRemoteDataSource interface and AuthRemoteDataSourceImpl
       - Constructor takes Dio HTTP client
       - Method: Future<UserModel> login(String email, String password)
       - POST /auth/login endpoint
       - Handle HTTP errors (401, 403, 500) throw specific exceptions
       - Parse response JSON to UserModel
    
    3. Create lib/features/auth/data/datasources/auth_local_datasource.dart:
       - AuthLocalDataSource interface and AuthLocalDataSourceImpl
       - Constructor takes CacheService (Hive)
       - Methods:
         - Future<void> cacheUser(UserModel user)
         - Future<UserModel?> getCachedUser()
         - Future<void> clearCache()
    
    4. Create lib/core/services/auth_storage_service.dart:
       - AuthStorageService class (uses SecureStorageService internally)
       - Methods:
         - Future<void> saveToken(String token)
         - Future<String?> getToken()
         - Future<void> saveUser(UserModel user) - JSON serialized
         - Future<UserModel?> getUser()
         - Future<void> clearAll()
       - Use AppConstants keys (jwt_token, user_id)
    
    5. Create lib/features/auth/data/repositories/auth_repository_impl.dart:
       - AuthRepositoryImpl implements AuthRepository
       - Dependencies: AuthRemoteDataSource, AuthLocalDataSource, AuthStorageService
       - login():
         - Call remoteDataSource.login()
         - On success: save token via authStorageService.saveToken(), cache user
         - On failure: map exceptions to Failures (AuthFailure, ServerFailure)
       - getCurrentUser():
         - First check secure storage, then fallback to cache
         - Return User entity (not model)
       - isAuthenticated(): Check if token exists in secure storage
       - logout(): Clear tokens and cached user
       - All methods return Result type (Success/Failure)
    
    6. Update lib/core/di/injection.dart:
       - Add authRepositoryProvider using Provider<AuthRepository>
       - Add authRemoteDataSourceProvider
       - Add authLocalDataSourceProvider
       - Add authStorageServiceProvider
       - Wire dependencies: data sources -> repository
    
    7. Run code generation: `flutter pub run build_runner build --delete-conflicting-outputs`
    
    Data layer rules:
    - Models extend entities
    - Data sources handle specific storage (API vs local)
    - Repository coordinates between sources
    - Exceptions caught and mapped to Failures
  </action>
  <verify>
    `flutter pub run build_runner build` completes successfully
    `flutter analyze` shows no errors
    Generated files exist: user_model.g.dart
  </verify>
  <done>
    UserModel with code generation, remote and local data sources, repository implementation with token storage, all providers configured, code generated
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Login Screen with Material 3 UI and Password Toggle</name>
  <files>
    - lib/features/auth/presentation/providers/auth_provider.dart
    - lib/features/auth/presentation/screens/login_screen.dart
    - lib/features/auth/presentation/widgets/password_field.dart
  </files>
  <action>
    Implement presentation layer with Material 3 design:
    
    1. Create lib/features/auth/presentation/providers/auth_provider.dart:
       - Define AuthState class with freezed (initial, loading, authenticated, error)
       - authStateProvider: StateNotifierProvider<AuthNotifier, AuthState>
       - AuthNotifier with methods:
         - login(String email, String password): calls LoginUseCase, updates state
         - checkAuthStatus(): check if user is already logged in on app start
         - logout(): clear auth state
       - Use ref.read(loginUseCaseProvider) to get use case
       - Handle loading state, success navigation, error display
    
    2. Create lib/features/auth/presentation/widgets/password_field.dart:
       - PasswordField widget (StatefulWidget)
       - Props: TextEditingController controller, String? errorText
       - Material 3 TextField with:
         - obscureText state (toggled with eye icon)
         - Suffix IconButton with eye/eye_off icons
         - Label: "Password"
         - Outlined border style (Material 3)
         - Error state support
    
    3. Create lib/features/auth/presentation/screens/login_screen.dart:
       - LoginScreen extends ConsumerWidget
       - Layout:
         - SafeArea with Scaffold
         - Centered SingleChildScrollView for keyboard handling
         - App logo/icon at top (placeholder Icon for now)
         - "Welcome Back" headline (headlineMedium - Material 3)
         - "Sign in to continue" subtitle (bodyLarge)
         - Email TextField:
           - keyboardType: TextInputType.emailAddress
           - Label: "Email"
           - Outlined style
           - Validation on submit
         - PasswordField (from widget above)
         - Login button:
           - FilledButton.tonal (Material 3)
           - Shows CircularProgressIndicator when loading
           - Full width (min height 48dp)
           - Text: "Sign In"
         - "Forgot Password?" text button (for future implementation)
       - State handling:
         - ref.watch(authStateProvider) to get current state
         - On loading: show progress indicator on button
         - On error: show SnackBar with error message and retry
         - On authenticated: navigation handled by parent
       - Material 3 styling:
         - Use Theme.of(context).colorScheme
         - Dynamic color support (follows system)
         - Proper elevation and spacing
    
    4. Update lib/core/app/app.dart:
       - Replace placeholder with LoginScreen
       - Wrap in AuthWrapper (checks auth status on startup)
       - AuthWrapper: ConsumerWidget that watches authStateProvider
       - On init: call ref.read(authProvider.notifier).checkAuthStatus()
       - Shows loading spinner while checking, then LoginScreen or Home
    
    Presentation layer rules:
    - ConsumerWidget/ConsumerStatefulWidget for Riverpod
    - UI reflects state immediately (loading states)
    - Error handling shows clear messages
    - Material 3 components and styling throughout
  </action>
  <verify>
    `flutter analyze` shows no errors
    `flutter build apk --debug` completes successfully
    Login screen is visually accessible (not just placeholder)
  </verify>
  <done>
    Login screen with Material 3 design, password visibility toggle, loading states, error handling, auth provider manages state, app shows login on launch
  </done>
</task>

</tasks>

<verification>
1. **Login UI Verification**: Screen shows email field, password field with eye toggle, sign in button
2. **Loading State Verification**: Button shows CircularProgressIndicator during login
3. **Error Handling Verification**: Failed login shows SnackBar with clear message
4. **Token Storage Verification**: After successful login, token is stored (check secure storage implementation)
5. **Session Persistence Verification**: Close app, reopen - should check auth status on startup
6. **Material 3 Verification**: Uses filled tonal buttons, outlined text fields, proper typography
7. **Integration Verification**: Full flow works: UI -> Provider -> Use Case -> Repository -> Data Source
8. **Code Quality**: `flutter analyze` passes, no critical warnings
</verification>

<success_criteria>
1. **Login Functionality** (AUTH-01):
   - User can enter email and password
   - Login button triggers authentication flow
   - Credentials validated (email format, password length)
   - API call made to authenticate user
   - Success: User entity received and stored

2. **Password Visibility** (AUTH-02):
   - Eye icon visible in password field
   - Tap toggles between visible/hidden
   - Icon changes between eye and eye_off
   - State persists while typing

3. **Loading States** (AUTH-07, UI-04):
   - CircularProgressIndicator shown during login API call
   - Button disabled or shows loading state
   - Loading indicator uses Material 3 style

4. **Error Handling** (AUTH-08, UI-05):
   - Invalid credentials show "Invalid email or password"
   - Network errors show "Connection failed. Please try again."
   - Empty fields show inline validation messages
   - SnackBar displays errors with retry action

5. **Session Persistence** (AUTH-03):
   - JWT token stored securely (flutter_secure_storage)
   - User data cached in Hive
   - App checks auth status on startup
   - Authenticated users skip login screen

6. **Material 3 Design** (UI-01):
   - Uses Material 3 components (FilledButton.tonal, Outlined TextField)
   - Follows Material 3 color scheme
   - Proper elevation and spacing
   - Typography hierarchy (headlineMedium, bodyLarge)

7. **Architecture Compliance**:
   - Domain layer has no Flutter/HTTP dependencies
   - Data layer implements repository interface
   - Presentation layer uses Riverpod for state
   - Dependencies injected via providers
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-02-SUMMARY.md`

Summary should include:
- Domain layer structure (entities, repository interface, use case)
- Data layer implementation (models, data sources, repository)
- Presentation layer components (screens, widgets, providers)
- Auth flow diagram: UI -> Provider -> UseCase -> Repository -> DataSource
- Token storage mechanism
- State management pattern (Riverpod)
- Next plan dependencies (Plan 03 needs navigation structure)
</output>
