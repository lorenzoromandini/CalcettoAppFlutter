---
phase: 01-foundation-auth
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/services/auth_storage_service.dart
  - lib/features/auth/data/repositories/auth_repository_impl.dart
  - lib/features/auth/presentation/screens/login_screen.dart
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "After email/password login, credentials stored in flutter_secure_storage"
    - "On biometric success, credentials retrieved and login use case called"
    - "User is authenticated and navigated to home (not logged out)"
    - "Biometric preference remains enabled"
  artifacts:
    - path: "lib/core/services/auth_storage_service.dart"
      provides: "Secure credential storage/retrieval"
      exports: ["storeCredentials", "getCredentials", "clearCredentials"]
    - path: "lib/features/auth/data/repositories/auth_repository_impl.dart"
      provides: "Credential storage after successful login"
      contains: "AuthStorageService.storeCredentials"
    - path: "lib/features/auth/presentation/screens/login_screen.dart"
      provides: "Fixed biometric login flow"
      pattern: "_attemptBiometricLogin.*AuthStorageService.*getCredentials"
  key_links:
    - from: "AuthRepositoryImpl"
      to: "AuthStorageService"
      via: "storeCredentials call after login success"
      pattern: "authStorageService.storeCredentials"
    - from: "LoginScreen._attemptBiometricLogin()"
      to: "AuthStorageService"
      via: "getCredentials retrieval"
      pattern: "ref.read(authStorageProvider).getCredentials()"
    - from: "LoginScreen._attemptBiometricLogin()"
      to: "LoginUseCase"
      via: "authenticate with retrieved credentials"
      pattern: "loginUseCase.call.*email.*password"
---

# Phase 01 Plan 05: Fix Biometric Login Flow (Gap Closure)

## Objective

Fix the critical bug in biometric authentication where line 67 in login_screen.dart calls `logout()` instead of authenticating the user. Implement secure credential storage/retrieval flow to enable proper biometric login after first email/password login.

**Purpose:** Close the AUTH-04 gap - biometric login currently logs user OUT instead of IN.

**Output:** Working biometric login flow that retrieves stored credentials and authenticates user.

**Gap Source:** 01-VERIFICATION.md - Truth #2 partial (biometric login flow broken)

## Execution Context

@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-04-SUMMARY.md

@lib/core/services/auth_storage_service.dart
@lib/features/auth/data/repositories/auth_repository_impl.dart
@lib/features/auth/presentation/screens/login_screen.dart
@lib/core/services/biometric_service.dart
@lib/features/auth/presentation/providers/biometric_provider.dart

## Tasks

<task type="auto">
  <name>Task 1: Add secure credential storage to AuthStorageService</name>
  <files>lib/core/services/auth_storage_service.dart</files>
  <action>
    Modify AuthStorageService to store and retrieve user credentials securely using flutter_secure_storage.
    
    Add three methods:
    1. `storeCredentials(String email, String password)` - stores credentials with key 'stored_credentials' as JSON
    2. `getCredentials()` - retrieves stored credentials, returns {email, password} or null
    3. `clearCredentials()` - removes stored credentials on logout
    
    Use FlutterSecureStorage (already imported) with:
    - Key: 'stored_credentials'
    - Format: JSON string {'email': email, 'password': password}
    - Handle errors gracefully (return null on failure)
    
    Ensure the class is exported from lib/core/services/ and provider is updated if needed.
  </action>
  <verify>Run `flutter analyze` - no errors in auth_storage_service.dart</verify>
  <done>AuthStorageService has storeCredentials, getCredentials, clearCredentials methods. Flutter analyze passes.</done>
</task>

<task type="auto">
  <name>Task 2: Store credentials after successful email/password login</name>
  <files>lib/features/auth/data/repositories/auth_repository_impl.dart</files>
  <action>
    Modify AuthRepositoryImpl.login() to store credentials after successful authentication.
    
    After successful login (result is success), call:
    ```dart
    await authStorageService.storeCredentials(email, password);
    ```
    
    Before storing, ensure AuthStorageService is available in the repository. 
    If authStorageService is not injected, add it as constructor dependency:
    - Add `required this.authStorageService` to constructor
    - Add `final AuthStorageService authStorageService;` field
    - Update provider instantiation in auth_provider.dart if needed
    
    Also ensure clearCredentials() is called in logout() method.
  </action>
  <verify>Grep for `storeCredentials` in auth_repository_impl.dart - should appear after login success</verify>
  <done>AuthRepositoryImpl calls storeCredentials after successful login. Logout clears credentials.</done>
</task>

<task type="auto">
  <name>Task 3: Fix biometric login flow (critical bug fix)</name>
  <files>lib/features/auth/presentation/screens/login_screen.dart</files>
  <action>
    Replace the broken logout() call at line 67 with proper biometric authentication flow.
    
    Modify `_attemptBiometricLogin()` method:
    1. After successful biometric authentication (authenticated == true):
       - Call `ref.read(authStorageProvider).getCredentials()` to retrieve stored credentials
       - If credentials exist (email and password):
         - Set loading state: `isLoading = true`
         - Call login use case: `await ref.read(loginUseCaseProvider).call(email: email, password: password)`
         - On success: navigate to home screen using GoRouter: `context.go('/home')`
         - On failure: show error message via SnackBar or bottom sheet
       - If credentials don't exist (first biometric use before email login):
         - Show error: "Please log in with email and password first to enable biometric login"
    
    2. The CRITICAL FIX: Replace line 67's `ref.read(authStateProvider.notifier).logout()` with the above flow.
    
    Ensure proper imports:
    - AuthStorageService provider
    - LoginUseCase provider  
    - GoRouter context extension
    
    The user should now be AUTHENTICATED and NAVIGATED TO HOME after biometric success, not logged out.
  </action>
  <verify>
    1. Grep for `logout()` in login_screen.dart - should NOT appear in _attemptBiometricLogin()
    2. Grep for `getCredentials` - should appear in _attemptBiometricLogin()
    3. Grep for `loginUseCase` - should appear after getCredentials call
  </verify>
  <done>Line 67 now retrieves credentials and calls login use case. No logout() call in biometric flow. User navigates to home on success.</done>
</task>

## Verification

### Code Verification
- [ ] `flutter analyze` passes with no errors
- [ ] grep `storeCredentials` in auth_repository_impl.dart returns result
- [ ] grep `getCredentials` in login_screen.dart returns result in _attemptBiometricLogin
- [ ] grep `logout()` in login_screen.dart - should NOT appear in _attemptBiometricLogin method
- [ ] grep `loginUseCase` in login_screen.dart appears after biometric success

### Gap Closure Verification
- [ ] Truth "User can enable biometric authentication after first login" now VERIFIED (not partial)
- [ ] Line 67 no longer calls logout() - replaced with credential retrieval + login flow
- [ ] Credentials are stored after email/password login
- [ ] Credentials are retrieved after biometric success
- [ ] Login use case is called with retrieved credentials
- [ ] User is authenticated and navigated to home (not logged out)

## Success Criteria

1. **Observable:** After email/password login, credentials stored in flutter_secure_storage
2. **Observable:** After biometric success, credentials retrieved and used for login
3. **Observable:** User is logged IN (not OUT) and navigated to home screen
4. **Observable:** Biometric preference toggle remains functional in settings
5. **Code:** `flutter analyze` passes, no type errors, all imports resolve

## Output

After completion, create `.planning/phases/01-foundation-auth/01-05-SUMMARY.md`

Document:
- Credentials storage/retrieval implementation
- Line 67 bug fix details
- Files modified with line counts
- Verification that AUTH-04 is now complete
