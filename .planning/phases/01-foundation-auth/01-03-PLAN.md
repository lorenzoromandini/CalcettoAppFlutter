---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - lib/core/app/app.dart
  - lib/core/theme/app_theme.dart
  - lib/core/theme/theme_provider.dart
  - lib/features/home/presentation/screens/home_screen.dart
  - lib/features/home/presentation/screens/clubs_screen.dart
  - lib/features/home/presentation/screens/matches_screen.dart
  - lib/features/home/presentation/screens/profile_screen.dart
  - lib/features/home/presentation/widgets/bottom_nav_bar.dart
  - lib/features/home/presentation/providers/navigation_provider.dart
  - lib/features/profile/presentation/screens/settings_screen.dart
  - lib/features/profile/presentation/widgets/logout_button.dart
autonomous: true
user_setup: []
must_haves:
  truths:
    - "User sees welcome screen on first launch"
    - "User sees bottom navigation with 4 items: Home, Clubs, Matches, Profile"
    - "Bottom navigation switches between screens with Material transitions"
    - "App respects system dark/light theme preference"
    - "User can log out from Profile screen"
    - "Screen transitions are smooth at 60fps"
    - "Navigation state persists across app restarts"
  artifacts:
    - path: "lib/core/theme/app_theme.dart"
      provides: "Material 3 theme with dynamic colors and dark/light modes"
      exports: ["lightTheme", "darkTheme", "AppTheme"]
    - path: "lib/features/home/presentation/widgets/bottom_nav_bar.dart"
      provides: "Bottom navigation bar with 4 destinations"
      min_lines: 80
      contains: ["NavigationBar", "NavigationDestination", "Home", "Clubs", "Matches", "Profile"]
    - path: "lib/features/home/presentation/providers/navigation_provider.dart"
      provides: "Riverpod state for navigation index"
      exports: ["navigationIndexProvider"]
    - path: "lib/features/profile/presentation/widgets/logout_button.dart"
      provides: "Logout button that clears auth and returns to login"
      contains: ["onPressed.*logout", "AuthNotifier"]
  key_links:
    - from: "lib/core/app/app.dart"
      to: "lib/core/theme/theme_provider.dart"
      via: "ThemeMode from provider"
      pattern: "themeMode.*ref\.watch"
    - from: "lib/core/theme/theme_provider.dart"
      to: "lib/core/theme/app_theme.dart"
      via: "ThemeData selection"
      pattern: "lightTheme|darkTheme"
    - from: "lib/features/home/presentation/widgets/bottom_nav_bar.dart"
      to: "lib/features/home/presentation/providers/navigation_provider.dart"
      via: "Current index state"
      pattern: "ref\.watch\(navigationIndexProvider\)"
    - from: "lib/features/profile/presentation/widgets/logout_button.dart"
      to: "lib/features/auth/presentation/providers/auth_provider.dart"
      via: "Logout action"
      pattern: "ref\.read\(authProvider.*notifier\)"
---

<objective>
Implement navigation system with bottom navigation bar, Material 3 theme with dark/light mode, welcome screen for first launch, and logout functionality.

Purpose: Complete the foundation with navigation structure, theming system, and user flow from first launch through authenticated experience. This delivers the complete Phase 1 user experience.

Output: Fully functional app with bottom navigation, theme switching, smooth transitions, welcome screen, and logout capability.
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-02-SUMMARY.md

# Plan 02 Dependencies (must exist before this plan runs)
- lib/features/auth/presentation/providers/auth_provider.dart
- lib/features/auth/presentation/screens/login_screen.dart
- lib/core/app/app.dart (will be modified to add navigation)
- lib/core/di/injection.dart (for provider setup)

# Phase 1 Requirements Being Implemented
- AUTH-04: User can use biometric login (Face ID/Touch ID) after first login (PARTIAL - deferred to future)
- AUTH-05: User can log out from any screen
- AUTH-06: User can request password reset via email (DEFERRED - not in this plan)
- UI-01: App follows Material 3 design system
- UI-02: App supports dark/light theme (system preference)
- UI-03: App uses bottom navigation for main sections
- UI-08: App is responsive across phone sizes
- PERF-01: App launches in under 3 seconds
- PERF-02: Screen transitions run at 60fps

# Locked Decisions (from CONTEXT.md)
- Bottom navigation bar with 4 items: Home, Clubs, Matches, Profile
- Default to system dark/light mode preference
- User can override theme in settings (in this plan: just system preference)
- Standard Material screen transitions (fade/slide)
- Responsive layouts for various phone sizes
- Navigation state persists across app restarts
- Welcome screen displayed on first app launch
- Then proceed to login
- Onboarding/tutorial can be added in future phase

# Claude's Discretion
- App bar styling: Use Material 3 NavigationBar with proper elevation
- Exact transition animations: Use Material PageRoute transitions (fade/slide)
- Welcome screen content: Simple welcome message with app logo
- Theme implementation: Use Material 3 dynamic color scheme
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Material 3 Theme with Dark/Light Mode Support</name>
  <files>
    - lib/core/theme/app_theme.dart
    - lib/core/theme/theme_provider.dart
    - lib/core/app/app.dart
  </files>
  <action>
    Create comprehensive Material 3 theming system:
    
    1. Create lib/core/theme/app_theme.dart:
       - Define AppTheme class with static methods
       - lightTheme(): ThemeData with Material 3
         - useMaterial3: true
         - colorScheme: ColorScheme.fromSeed() with football-appropriate seed color
         - Suggested seed: Colors.green.shade700 (football field green) or Colors.deepPurple
         - Typography: Use Material 3 text theme
         - Component themes: filledButtonTheme, outlinedButtonTheme, inputDecorationTheme
       - darkTheme(): ThemeData.dark() with Material 3
         - Same seed color for consistency
         - Adjust brightness for dark mode
       - Component-specific styling:
         - Elevated buttons: proper elevation and colors
         - Cards: outlined style with proper elevation
         - Text fields: outlined border, focused states
         - App bars: Material 3 style (no elevation, surface color)
       - Constants:
         - borderRadius (12.0 for Material 3)
         - spacing values (8, 16, 24, 32)
         - animation durations
    
    2. Create lib/core/theme/theme_provider.dart:
       - ThemeModeNotifier extends StateNotifier<ThemeMode>
       - Initial state: ThemeMode.system (respects OS preference)
       - Methods:
         - setSystem(): set to ThemeMode.system
         - setLight(): set to ThemeMode.light
         - setDark(): set to ThemeMode.dark
       - Provider: themeModeProvider (StateNotifierProvider)
       - Persistence: Save preference to Hive cache
       - On init: Load saved preference from cache, default to system
    
    3. Update lib/core/app/app.dart:
       - MaterialApp configuration:
         - theme: AppTheme.lightTheme()
         - darkTheme: AppTheme.darkTheme()
         - themeMode: ref.watch(themeModeProvider)
         - debugShowCheckedModeBanner: false
       - Keep ProviderScope wrapping
       - Set initial route based on auth status
       
    Material 3 specific patterns:
    - Use ColorScheme.fromSeed() for dynamic colors
    - Surface colors for backgrounds, primary for actions
    - Outlined style for input fields
    - Filled tonal buttons for secondary actions
    - No app bar shadow (Material 3 uses surface tint)
  </action>
  <verify>
    `flutter analyze` shows no errors
    App compiles and runs
    Check that theme changes when system theme changes (test on device/simulator)
  </verify>
  <done>
    Material 3 themes defined with seed colors, theme provider manages system/light/dark modes, app responds to theme changes, proper component theming
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Bottom Navigation with 4 Destinations and Screen Structure</name>
  <files>
    - lib/features/home/presentation/screens/home_screen.dart
    - lib/features/home/presentation/screens/clubs_screen.dart
    - lib/features/home/presentation/screens/matches_screen.dart
    - lib/features/home/presentation/screens/profile_screen.dart
    - lib/features/home/presentation/widgets/bottom_nav_bar.dart
    - lib/features/home/presentation/providers/navigation_provider.dart
    - lib/core/app/app.dart
  </files>
  <action>
    Implement bottom navigation structure with 4 main sections:
    
    1. Create lib/features/home/presentation/providers/navigation_provider.dart:
       - StateNotifier<NavigationState> with currentIndex
       - NavigationState class with index and route
       - Provider: navigationProvider
       - Persistence: Save index to cache (optional, for state restore)
    
    2. Create lib/features/home/presentation/widgets/bottom_nav_bar.dart:
       - BottomNavBar extends ConsumerWidget
       - Material 3 NavigationBar (not BottomNavigationBar - that's Material 2)
       - 4 NavigationDestination items:
         - Home: Icon(Icons.home_outlined), selected: Icons.home, label: "Home"
         - Clubs: Icon(Icons.groups_outlined), selected: Icons.groups, label: "Clubs"
         - Matches: Icon(Icons.sports_soccer_outlined), selected: Icons.sports_soccer, label: "Matches"
       - Profile: Icon(Icons.person_outline), selected: Icons.person, label: "Profile"
       - Current index from ref.watch(navigationProvider)
       - onDestinationSelected: update provider state
       - Material 3 style: surface color, proper elevation, indicator
    
    3. Create placeholder screens (will be implemented in future phases):
       - lib/features/home/presentation/screens/home_screen.dart:
         - Scaffold with AppBar (title: "Home")
         - Body: Center with placeholder text "Home Screen - Coming Soon"
         - Material 3 styling
       
       - lib/features/home/presentation/screens/clubs_screen.dart:
         - Scaffold with AppBar (title: "My Clubs")
         - Body: Center with placeholder text "Clubs Screen - Coming Soon"
       
       - lib/features/home/presentation/screens/matches_screen.dart:
         - Scaffold with AppBar (title: "Matches")
         - Body: Center with placeholder text "Matches Screen - Coming Soon"
       
       - lib/features/home/presentation/screens/profile_screen.dart:
         - Scaffold with AppBar (title: "Profile")
         - Body: Profile content with logout button
    
    4. Create MainLayout widget in lib/features/home/presentation/screens/main_layout.dart:
       - ConsumerWidget with bottom navigation
       - IndexedStack for screen switching (preserves state)
       - Children: [HomeScreen(), ClubsScreen(), MatchesScreen(), ProfileScreen()]
       - BottomNavigationBar: BottomNavBar widget
       - Handles navigation index changes
    
    5. Update lib/core/app/app.dart:
       - Add routing logic:
         - If authenticated: show MainLayout
         - If not authenticated: show LoginScreen
       - Use ConsumerWidget to watch authStateProvider
       - Ensure smooth transition between login and main app
       - Use MaterialPageRoute with proper transitions
    
    Navigation patterns:
    - IndexedStack preserves scroll position and state
    - NavigationBar is Material 3 equivalent
    - Each screen is independent Scaffold
    - Transitions handled by Flutter's default Material page transitions
  </action>
  <verify>
    `flutter analyze` shows no errors
    App shows bottom navigation with 4 items
    Tapping items switches screens
    Screen state preserved when switching
  </verify>
  <done>
    4 screen placeholders created, bottom navigation switches between them using IndexedStack, navigation provider manages state, Material 3 NavigationBar implemented
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Welcome Screen, Logout Functionality, and First Launch Flow</name>
  <files>
    - lib/features/auth/presentation/screens/welcome_screen.dart
    - lib/features/auth/presentation/providers/first_launch_provider.dart
    - lib/features/profile/presentation/screens/settings_screen.dart
    - lib/features/profile/presentation/widgets/logout_button.dart
    - lib/core/app/app.dart
  </files>
  <action>
    Complete the user flow with welcome screen and logout:
    
    1. Create lib/features/auth/presentation/providers/first_launch_provider.dart:
       - FirstLaunchNotifier extends StateNotifier<bool>
       - Check Hive for 'has_seen_welcome' key
       - Initial state: false (not seen)
       - After welcome shown: set to true and persist
       - Provider: firstLaunchProvider
    
    2. Create lib/features/auth/presentation/screens/welcome_screen.dart:
       - WelcomeScreen extends ConsumerWidget
       - Full-screen Material 3 design:
         - Gradient or solid background using theme colors
         - Large app icon/logo (placeholder: Icons.sports_soccer, size 120)
         - Headline: "Welcome to Calcetto" (headlineLarge)
         - Subtitle: "Manage your football matches and clubs" (bodyLarge)
         - Feature highlights (3 items with icons):
           - "Track matches and RSVPs"
           - "Manage your clubs"
           - "View player statistics"
         - "Get Started" button (FilledButton, full width)
       - Button action: mark welcome as seen, navigate to login
       - Responsive: Works on small and large phones
    
    3. Create lib/features/profile/presentation/widgets/logout_button.dart:
       - LogoutButton widget (ConsumerWidget)
       - Appearance: ListTile with logout icon
       - Icon: Icons.logout
       - Title: "Log Out"
       - Subtitle: "Sign out of your account"
       - onTap: Show confirmation dialog, then logout
       - Confirmation dialog:
         - Title: "Log Out?"
         - Content: "Are you sure you want to sign out?"
         - Actions: Cancel, Log Out
       - On confirm: ref.read(authProvider.notifier).logout()
    
    4. Create lib/features/profile/presentation/screens/settings_screen.dart:
       - SettingsScreen extends ConsumerWidget
       - Scaffold with AppBar (title: "Settings")
       - Settings sections:
         - Account section with logout button
         - Theme section (future: add theme toggle)
         - About section with app version
       - Use Material 3 ListTile styling
       - Include LogoutButton widget
    
    5. Update lib/features/home/presentation/screens/profile_screen.dart:
       - Add "Settings" option that navigates to SettingsScreen
       - Basic profile info placeholder (avatar, name, email)
       - List of options: Settings, Help, About
       - Material 3 card styling
    
    6. Update lib/core/app/app.dart:
       - App startup flow:
         a. Check first launch: if !hasSeenWelcome -> WelcomeScreen
         b. Check auth: if !authenticated -> LoginScreen
         c. Show MainLayout (authenticated home)
       - Handle navigation:
         - WelcomeScreen -> LoginScreen (after Get Started)
         - LoginScreen -> MainLayout (after successful login)
         - Logout -> LoginScreen
       - Ensure proper transition animations between screens
    
    User flow:
    First install -> WelcomeScreen -> LoginScreen -> MainLayout (Home)
    Return visit -> Check auth -> MainLayout or LoginScreen
    Logout -> LoginScreen
    
    Performance considerations:
    - Welcome screen only shown once
    - Auth check is fast (secure storage read)
    - No delay for returning users
  </action>
  <verify>
    `flutter analyze` shows no errors
    App builds successfully
    Test flow:
    1. Clear app data, launch -> Welcome screen appears
    2. Tap Get Started -> Login screen
    3. Login -> Main layout with bottom nav
    4. Profile -> Settings -> Logout -> Login screen
    5. Re-launch app -> Should skip welcome, check auth
  </verify>
  <done>
    Welcome screen with Material 3 design, first launch detection, logout functionality with confirmation, profile screen with settings navigation, complete user flow implemented
  </done>
</task>

</tasks>

<verification>
1. **Welcome Screen Verification**: First launch shows welcome with "Get Started" button
2. **Theme Verification**: App respects system dark/light mode
3. **Bottom Navigation Verification**: 4 items visible (Home, Clubs, Matches, Profile)
4. **Navigation Verification**: Tapping items switches screens with smooth transitions
5. **Logout Verification**: Profile -> Settings -> Logout shows confirmation, returns to login
6. **Auth Flow Verification**: Login -> Home, Logout -> Login, Welcome (once) -> Login
7. **Material 3 Verification**: NavigationBar, proper colors, typography, elevation
8. **Performance Verification**: App launches in under 3 seconds, transitions are smooth
9. **Persistence Verification**: Welcome shown only once, auth state persists
</verification>

<success_criteria>
1. **Welcome Screen**:
   - Shows on first app launch only
   - Displays app branding and feature highlights
   - "Get Started" button proceeds to login
   - Material 3 design with proper spacing and typography

2. **Bottom Navigation** (UI-03):
   - 4 destinations: Home, Clubs, Matches, Profile
   - NavigationBar widget (Material 3)
   - Icons and labels clearly visible
   - Selected state with indicator animation
   - Thumb-friendly touch targets (48dp minimum)

3. **Theme System** (UI-01, UI-02):
   - Follows Material 3 design system throughout
   - Respects system dark/light mode by default
   - Dynamic color scheme from seed color
   - Proper contrast in both modes
   - Component theming (buttons, inputs, cards)

4. **Navigation Flow**:
   - First launch: Welcome -> Login -> Main App
   - Returning user: Login (if not auth'd) or Main App (if auth'd)
   - Bottom nav switches screens instantly
   - State preserved when switching tabs (IndexedStack)

5. **Logout** (AUTH-05):
   - Logout option in Profile -> Settings
   - Confirmation dialog before logout
   - Clears auth tokens from secure storage
   - Returns to login screen
   - Can log back in immediately

6. **Screen Transitions** (PERF-02):
   - Bottom nav switches: instant (no animation needed)
   - Page navigations: Material fade/slide transitions
   - Runs at 60fps on target devices
   - No jank or dropped frames

7. **Launch Performance** (PERF-01):
   - Cold start under 3 seconds
   - Warm start under 1 second
   - Welcome/login check doesn't block UI
   - Services initialized asynchronously

8. **Responsiveness** (UI-08):
   - Layouts adapt to different phone sizes
   - Bottom nav works on small screens (320dp+)
   - Text scales appropriately
   - No overflow errors on small screens
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`

Summary should include:
- Theme implementation details (seed color, color scheme)
- Navigation structure (IndexedStack, NavigationBar)
- Screen hierarchy and routing
- User flow diagram
- First launch detection mechanism
- Logout implementation
- Performance optimization notes
- Phase 1 completion status

Update ROADMAP.md to mark Phase 1 as "In Progress" or "Completed" based on execution.
</output>
