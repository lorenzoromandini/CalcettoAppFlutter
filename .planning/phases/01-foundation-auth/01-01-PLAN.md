---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/main.dart
  - lib/core/app/app.dart
  - lib/core/constants/app_constants.dart
  - lib/core/errors/exceptions.dart
  - lib/core/errors/failures.dart
  - lib/core/utils/result.dart
  - lib/core/utils/type_definitions.dart
  - android/app/build.gradle
  - ios/Runner/Info.plist
autonomous: true
user_setup:
  - service: Flutter SDK
    why: "Flutter development environment"
    steps:
      - "Ensure Flutter 3.24+ is installed: flutter --version"
      - "Run flutter doctor to verify setup"
      - "Configure Android SDK for Android builds"
      - "Configure Xcode for iOS builds (macOS only)"
must_haves:
  truths:
    - "Flutter project runs without errors"
    - "Clean Architecture folder structure exists"
    - "Core utilities (Result, Failures, Exceptions) are available"
    - "App launches and shows placeholder home screen"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Dependencies configured (Riverpod, Dio, Hive, flutter_secure_storage)"
      contains: ["flutter_riverpod:", "dio:", "hive:", "hive_flutter:", "flutter_secure_storage:", "local_auth:"]
    - path: "lib/core/utils/result.dart"
      provides: "Functional Result type for error handling"
      exports: ["Result", "Success", "Failure"]
    - path: "lib/core/errors/failures.dart"
      provides: "Domain failure types"
      contains: ["class Failure", "class AuthFailure", "class ServerFailure", "class CacheFailure"]
    - path: "lib/core/app/app.dart"
      provides: "Root app widget with ProviderScope"
      min_lines: 30
  key_links:
    - from: "lib/core/app/app.dart"
      to: "lib/main.dart"
      via: "runApp()"
      pattern: "runApp.*ProviderScope"
    - from: "lib/core/utils/result.dart"
      to: "Domain layer"
      via: "Return type in use cases"
      pattern: "Future\u003cResult"
---

<objective>
Initialize Flutter project with Clean Architecture structure and configure core dependencies.

Purpose: Establish the foundation for the entire application with proper architecture, dependency injection, and error handling patterns. This plan creates the scaffolding that all subsequent plans depend on.

Output: A running Flutter project with Clean Architecture folder structure, core utilities, and all required dependencies configured.
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 1 Requirements
- AUTH-01: User can log in with email and password
- AUTH-03: User session persists across app restarts
- UI-01: App follows Material 3 design system
- PERF-01: App launches in under 3 seconds

# Technology Stack (from CONTEXT.md)
- Flutter 3.24+
- Riverpod 2.x for state management
- Dio 5.x for HTTP client
- Hive for local caching
- flutter_secure_storage for JWT
- Material 3 design system
- Clean Architecture pattern
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Flutter Project and Configure Dependencies</name>
  <files>
    - pubspec.yaml
    - android/app/build.gradle
    - ios/Runner/Info.plist
  </files>
  <action>
    1. Initialize new Flutter project if not exists: `flutter create calcetto_app --platforms android,ios --org com.calcetto`
    
    2. Update pubspec.yaml with required dependencies:
       - flutter_riverpod: ^2.4.0 (state management)
       - dio: ^5.4.0 (HTTP client)
       - hive: ^2.2.3 (local database)
       - hive_flutter: ^1.1.0 (Hive Flutter integration)
       - flutter_secure_storage: ^9.0.0 (secure token storage)
       - local_auth: ^2.2.0 (biometric authentication)
       - dartz: ^0.10.1 (functional programming - Result type)
       - freezed_annotation: ^2.4.0 (immutable classes)
       - json_annotation: ^4.8.0 (JSON serialization)
       - google_fonts: ^6.1.0 (Material 3 typography)
    
    3. Add dev dependencies:
       - build_runner: ^2.4.7
       - freezed: ^2.4.6
       - json_serializable: ^6.7.1
       - hive_generator: ^2.0.1
    
    4. Configure Android for secure storage (minSdkVersion 23) in android/app/build.gradle
    
    5. Configure iOS for local auth in ios/Runner/Info.plist:
       - Add NSFaceIDUsageDescription for biometric authentication
    
    6. Run `flutter pub get` to install dependencies
    
    NOTE: Do NOT run code generation yet - that happens in Task 2
  </action>
  <verify>
    Run `flutter pub deps` and confirm all packages are installed without version conflicts
  </verify>
  <done>
    pubspec.yaml contains all required dependencies, flutter pub get completes successfully, no version conflicts
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Clean Architecture Folder Structure and Core Utilities</name>
  <files>
    - lib/core/app/app.dart
    - lib/core/constants/app_constants.dart
    - lib/core/errors/exceptions.dart
    - lib/core/errors/failures.dart
    - lib/core/utils/result.dart
    - lib/core/utils/type_definitions.dart
    - lib/main.dart
  </files>
  <action>
    Create the Clean Architecture folder structure:
    
    1. Create directory structure:
       - lib/core/
         - app/ (app widget, provider setup)
         - constants/ (app-wide constants)
         - errors/ (exceptions and failures)
         - utils/ (utility classes)
       - lib/features/
         - auth/ (authentication feature)
       - lib/data/
         - models/
         - datasources/
       - lib/domain/
         - entities/
         - repositories/
         - usecases/
    
    2. Create lib/core/utils/result.dart:
       - Implement Result<T, E> sealed class with Success and Failure variants
       - Add fold, map, flatMap methods for functional composition
       - Use this for all use case return types (not exceptions)
    
    3. Create lib/core/errors/failures.dart:
       - Define abstract Failure class with message property
       - Create concrete failures: AuthFailure, ServerFailure, CacheFailure, NetworkFailure
       - Each with specific error messages for UI display
    
    4. Create lib/core/errors/exceptions.dart:
       - Define custom exceptions: AuthException, ServerException, CacheException
       - These are thrown by data sources, caught by repositories
    
    5. Create lib/core/utils/type_definitions.dart:
       - Define common type aliases: JsonMap = Map<String, dynamic>
       - Define ResultFuture<T> = Future<Result<T, Failure>>
    
    6. Create lib/core/constants/app_constants.dart:
       - API base URL placeholder
       - Storage key constants (jwt_token, refresh_token, user_id)
       - Biometric preference key
    
    7. Create lib/core/app/app.dart:
       - MaterialApp wrapped in ProviderScope
       - Material 3 enabled: themeMode: ThemeMode.system, useMaterial3: true
       - Home widget placeholder (Container with Text "CalcettoApp"))
    
    8. Update lib/main.dart:
       - Initialize Hive: await Hive.initFlutter()
       - Initialize secure storage
       - Run app with CalcettoApp widget
    
    Pattern to follow: Presentation -> Domain <- Data (dependencies point inward)
    Do NOT create feature implementations yet - just the folder structure and core utilities.
  </action>
  <verify>
    Run `flutter analyze` - should show no errors (only warnings about unused imports expected)
    Run `flutter build apk --debug` completes successfully (verifies Android build configuration)
  </verify>
  <done>
    All core utility files exist with proper implementations, folder structure follows Clean Architecture, app builds without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Initialize Hive and Secure Storage Services</name>
  <files>
    - lib/core/services/secure_storage_service.dart
    - lib/core/services/cache_service.dart
    - lib/core/di/injection.dart
    - lib/main.dart
  </files>
  <action>
    Create abstraction layers for secure storage and caching:
    
    1. Create lib/core/services/secure_storage_service.dart:
       - Abstract SecureStorageService interface
       - FlutterSecureStorage implementation
       - Methods: writeToken(String key, String value), readToken(String key), deleteToken(String key)
       - Keys from AppConstants: jwt_token, refresh_token, user_id
    
    2. Create lib/core/services/cache_service.dart:
       - Abstract CacheService interface
       - HiveCacheService implementation
       - Generic methods: put<T>(String key, T value), get<T>(String key), delete(String key), clear()
       - Handle Hive box initialization and error cases
    
    3. Create lib/core/di/injection.dart:
       - Provider definitions using flutter_riverpod
       - secureStorageProvider: Provider<SecureStorageService>
       - cacheServiceProvider: Provider<CacheService>
       - Lazy initialization pattern
    
    4. Update lib/main.dart:
       - Open Hive boxes needed for auth: await Hive.openBox('auth')
       - Ensure services are initialized before app starts
       - Add error handling for initialization failures
    
    Do NOT create auth-specific storage yet - just the generic service abstractions.
  </action>
  <verify>
    Run `flutter test` (even if no tests exist, verifies project compiles)
    Check that main.dart initializes services before runApp()
  </verify>
  <done>
    Secure storage and cache services exist with interfaces, injection providers configured, main.dart initializes services, no compilation errors
  </done>
</task>

</tasks>

<verification>
1. **Build Verification**: `flutter build apk --debug` completes without errors
2. **Analysis Verification**: `flutter analyze` shows no critical errors
3. **Structure Verification**: Clean Architecture folders exist:
   - `ls lib/core/app/ lib/core/constants/ lib/core/errors/ lib/core/utils/ lib/core/services/ lib/core/di/`
4. **Dependencies Verification**: All pubspec dependencies resolve: `flutter pub deps | grep -E "(riverpod|dio|hive|secure_storage|local_auth)"`
5. **App Launch**: App launches to placeholder screen showing "CalcettoApp"
</verification>

<success_criteria>
1. Flutter project initializes and builds successfully
2. All required dependencies (Riverpod, Dio, Hive, flutter_secure_storage, local_auth) are configured
3. Clean Architecture folder structure is in place with clear separation:
   - core/ for shared utilities and services
   - features/ for feature-specific code
   - data/, domain/ for layered architecture
4. Core utilities implemented:
   - Result<T, Failure> type for functional error handling
   - Failure classes for domain errors
   - Exception classes for data layer
   - Type definitions for common patterns
5. Service abstractions created:
   - SecureStorageService for JWT tokens
   - CacheService for local data persistence
   - Riverpod providers for dependency injection
6. App launches without crashes and initializes all services
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`

Summary should include:
- Actual folder structure created
- Dependencies with versions
- Core utilities API signatures
- Service interfaces
- Next plan dependencies (Plan 02 needs injection providers and Result type)
</output>
