---
phase: 01-foundation-auth
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/services/biometric_service.dart
  - lib/features/auth/presentation/providers/biometric_provider.dart
  - lib/features/profile/presentation/screens/settings_screen.dart
  - lib/features/auth/presentation/screens/login_screen.dart
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User can check if device supports biometrics"
    - "User can enable/disable biometric login in settings"
    - "If enabled, user sees biometric login option on login screen"
    - "Biometric auth bypasses email/password entry"
    - "Biometric preference persisted in Hive"
  artifacts:
    - path: "lib/core/services/biometric_service.dart"
      provides: "Biometric authentication service using local_auth"
      exports: ["BiometricService"]
    - path: "lib/features/auth/presentation/providers/biometric_provider.dart"
      provides: "Biometric state management"
      exports: ["biometricEnabledProvider"]
    - path: "lib/features/profile/presentation/screens/settings_screen.dart"
      provides: "Settings screen with biometric toggle"
      contains: "BiometricToggle widget"
    - path: "lib/features/auth/presentation/screens/login_screen.dart"
      provides: "Login screen with biometric auth option"
      contains: "biometric login button"
  key_links:
    - from: "lib/features/auth/presentation/screens/login_screen.dart"
      to: "lib/core/services/biometric_service.dart"
      via: "BiometricService.authenticate()"
      pattern: "biometricService\\.authenticate"
    - from: "lib/features/profile/presentation/screens/settings_screen.dart"
      to: "lib/features/auth/presentation/providers/biometric_provider.dart"
      via: "provider watch/call"
      pattern: "ref\\.(watch|read).*biometric"
    - from: "lib/core/services/biometric_service.dart"
      to: "lib/core/constants/app_constants.dart"
      via: "storage key usage"
      pattern: "biometricEnabledKey"
---

<objective>
Implement biometric authentication (Face ID/Touch ID) allowing users to enable biometric login after first successful authentication.

Purpose: Close AUTH-04 gap - users can use biometric authentication instead of entering password every time.
Output: BiometricService, BiometricProvider, biometric toggle in settings, biometric login option on login screen.
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-auth/01-02-SUMMARY.md
@.planning/phases/01-foundation-auth/01-03-SUMMARY.md
@lib/core/constants/app_constants.dart
@lib/core/services/auth_storage_service.dart
@lib/features/auth/presentation/providers/auth_provider.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BiometricService using local_auth package</name>
  <files>lib/core/services/biometric_service.dart</files>
  <action>
    Create a new service file that wraps local_auth functionality:
    
    1. Import 'package:local_auth/local_auth.dart' and required Flutter services
    2. Create BiometricService class with private LocalAuth instance
    3. Implement method: `Future<bool> canCheckBiometrics()` - checks if device supports biometrics
    4. Implement method: `Future<bool> isBiometricAvailable()` - checks if biometrics are enrolled
    5. Implement method: `Future<bool> authenticate({String reason})` - prompts biometric auth with given reason
    6. Implement method: `Future<void> enableBiometric()` - saves preference to Hive using AppConstants.biometricEnabledKey
    7. Implement method: `Future<void> disableBiometric()` - removes preference from Hive
    8. Implement method: `Future<bool> isBiometricEnabled()` - checks Hive for biometric preference
    9. Use Hive box 'auth' (AppConstants.authBoxName) for storing biometric preference
    10. Handle PlatformException for devices without biometric capability
    11. Add proper error handling with Result pattern or try/catch returning bool
    
    Reference existing patterns from auth_storage_service.dart for Hive usage.
  </action>
  <verify>
    - Run: `flutter analyze lib/core/services/biometric_service.dart` - no errors
    - Verify file exists with 80-120 lines
    - Verify all 6 methods are implemented
    - Verify imports include local_auth and hive packages
  </verify>
  <done>
    BiometricService created with canCheckBiometrics(), isBiometricAvailable(), authenticate(), enableBiometric(), disableBiometric(), isBiometricEnabled() methods. Uses local_auth package and Hive for preference storage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create BiometricProvider for state management</name>
  <files>lib/features/auth/presentation/providers/biometric_provider.dart</files>
  <action>
    Create Riverpod provider for biometric state management:
    
    1. Import flutter_riverpod and BiometricService
    2. Create provider: `final biometricServiceProvider = Provider<BiometricService>((ref) => BiometricService())`
    3. Create StateNotifier class: BiometricNotifier extends StateNotifier<BiometricState>
    4. BiometricState is a sealed class using freezed with states: initial, checking, enabled, disabled, error
    5. Create provider: `final biometricEnabledProvider = StateNotifierProvider<BiometricNotifier, BiometricState>((ref) => BiometricNotifier(ref.watch(biometricServiceProvider)))`
    6. BiometricNotifier methods:
       - `checkBiometricSupport()` - calls service.canCheckBiometrics() and service.isBiometricAvailable(), updates state
       - `toggleBiometric(bool enabled)` - calls enableBiometric() or disableBiometric(), refreshes state
       - `loadBiometricStatus()` - calls isBiometricEnabled() on init
    7. In constructor, call loadBiometricStatus() to initialize state
    8. Handle errors by transitioning to error state with message
    9. Follow existing provider patterns from auth_provider.dart
    
    Use freezed for BiometricState if already in project, otherwise use simple bool state.
  </action>
  <verify>
    - Run: `flutter analyze lib/features/auth/presentation/providers/biometric_provider.dart` - no errors
    - Verify file exists with 60-100 lines
    - Verify biometricEnabledProvider exports StateNotifier<BiometricState>
    - Verify provider imports and uses BiometricService
  </verify>
  <done>
    BiometricProvider created with StateNotifier managing biometric enabled/disabled state. Provider integrates with BiometricService and persists to Hive.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add biometric toggle to settings and login screen</name>
  <files>
    lib/features/profile/presentation/screens/settings_screen.dart
    lib/features/auth/presentation/screens/login_screen.dart
  </files>
  <action>
    Modify two screens to integrate biometric authentication:
    
    **Settings Screen (settings_screen.dart):**
    1. Import biometric_provider.dart
    2. Convert SettingsScreen to ConsumerStatefulWidget (needs state for biometric status)
    3. Add biometric toggle SwitchListTile after the About tile, before Account section:
       - Label: "Enable Face ID" or "Enable Touch ID" (check platform in initState)
       - Use biometricEnabledProvider to watch state
       - Only show if device supports biometrics (check on init)
       - On toggle: call biometricEnabledProvider.notifier.toggleBiometric(value)
       - Show loading indicator while checking biometric availability
    4. Add platform detection in createState to determine Face ID vs Touch ID wording
    
    **Login Screen (login_screen.dart):**
    1. Import biometric_provider.dart and BiometricService
    2. Add a new button below "Forgot Password?" for biometric login:
       - Icon: Icons.face or Icons.fingerprint based on platform
       - Label: "Sign in with Face ID" or "Sign in with Touch ID"
       - Only show if biometric is enabled (check biometricEnabledProvider)
       - On tap: call BiometricService.authenticate(), on success trigger login flow
    3. Add biometric provider watch to check if biometric is enabled
    4. Handle biometric auth success by triggering authenticated state in authStateProvider
    
    Reference existing button styles from login_screen.dart for consistency.
  </action>
  <verify>
    - Run: `flutter analyze lib/features/profile/presentation/screens/settings_screen.dart` - no errors
    - Run: `flutter analyze lib/features/auth/presentation/screens/login_screen.dart` - no errors
    - Verify settings_screen.dart has SwitchListTile for biometric toggle
    - Verify login_screen.dart has biometric login button with icon
    - Verify both screens import and use biometric_provider.dart
  </verify>
  <done>
    Settings screen shows biometric toggle (conditional on device support). Login screen shows biometric login button when biometric is enabled. Both screens integrate with BiometricProvider.
  </done>
</task>

</tasks>

<verification>
- [ ] BiometricService has all 6 required methods implemented
- [ ] BiometricProvider manages state and integrates with service
- [ ] Settings screen has biometric toggle that persists to Hive
- [ ] Login screen has biometric login button that triggers authentication
- [ ] `flutter analyze` passes for all modified files
- [ ] Biometric preference stored in Hive box 'auth' with key 'biometric_enabled'
- [ ] local_auth package is used (verify in pubspec.yaml - already present at ^2.2.0)
</verification>

<success_criteria>
**Gap AUTH-04 CLOSED:**
- User can enable biometric authentication in settings screen (toggle appears if device supports it)
- User can disable biometric authentication in settings screen
- Biometric preference persisted in Hive
- When enabled, login screen shows "Sign in with Face ID/Touch ID" button
- Biometric authentication bypasses email/password entry on login screen
- BiometricService properly wraps local_auth package functionality

**Observable Truths Achieved:**
1. ✓ User can check if device supports biometrics (BiometricService.canCheckBiometrics())
2. ✓ User can enable/disable biometric login in settings (SwitchListTile toggle)
3. ✓ If enabled, user sees biometric login option on login screen (conditional button)
4. ✓ Biometric auth bypasses email/password entry (authenticate() method)
5. ✓ Biometric preference persisted in Hive (enableBiometric()/disableBiometric())
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-04-SUMMARY.md` with:
- Implemented artifacts list
- Biometric auth flow documentation
- Any platform-specific notes (iOS Face ID vs Android Touch ID)
- Verification results
</output>
