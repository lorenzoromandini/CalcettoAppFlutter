---
phase: 02-clubs-offline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/core/services/connectivity_service.dart
  - lib/core/providers/connectivity_provider.dart
  - lib/core/services/clubs_remote_datasource.dart
  - lib/core/services/clubs_local_datasource.dart
  - lib/features/clubs/data/models/club_model.dart
  - lib/features/clubs/data/models/club_model.g.dart
  - lib/features/clubs/data/models/member_model.dart
  - lib/features/clubs/data/models/member_model.g.dart
  - lib/features/clubs/domain/entities/club.dart
  - lib/features/clubs/domain/entities/member.dart
  - lib/features/clubs/domain/repositories/clubs_repository.dart
  - lib/features/clubs/data/repositories/clubs_repository_impl.dart
autonomous: true
must_haves:
  truths:
    - "App detects network connectivity changes in real-time"
    - "Clubs data is cached locally with 5-minute TTL"
    - "Repository implements cache-first strategy with fallback"
    - "Club and Member entities are immutable with freezed"
  artifacts:
    - path: "lib/core/services/connectivity_service.dart"
      provides: "Network connectivity detection using connectivity_plus"
      contains: ["ConnectivityService", "isOnline", "onConnectivityChanged"]
    - path: "lib/features/clubs/domain/entities/club.dart"
      provides: "Club entity with id, name, logoUrl, memberCount, userRole"
      contains: ["@freezed class Club", "ClubRole enum"]
    - path: "lib/features/clubs/domain/entities/member.dart"
      provides: "Member entity with id, name, avatarUrl, role, stats"
      contains: ["@freezed class Member", "MemberStats"]
    - path: "lib/features/clubs/data/repositories/clubs_repository_impl.dart"
      provides: "Network-aware repository with cache-first strategy"
      contains: ["getClubs()", "getClubById()", "cache-first pattern"]
  key_links:
    - from: "lib/core/services/connectivity_service.dart"
      to: "lib/features/clubs/data/repositories/clubs_repository_impl.dart"
      via: "Repository checks connectivity before network calls"
      pattern: "connectivity.isOnline"
    - from: "lib/features/clubs/data/repositories/clubs_repository_impl.dart"
      to: "lib/core/services/clubs_local_datasource.dart"
      via: "Cache clubs after successful fetch"
      pattern: "local.cacheClubs()"
    - from: "lib/features/clubs/domain/entities/club.dart"
      to: "lib/features/clubs/data/models/club_model.dart"
      via: "Model extends entity"
      pattern: "class ClubModel extends Club"
---

<objective>
Implement offline-first infrastructure: network detection service, clubs repository with cache-first strategy, and data models for clubs and members.

Purpose: Enable the app to work offline by detecting network state, caching clubs data locally with TTL invalidation, and providing a repository that seamlessly switches between network and cache. This is the foundation for all offline requirements.

Output: Connectivity service, clubs repository with offline support, Club/Member entities and models with Hive persistence.
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-05-SUMMARY.md
@.planning/phases/02-clubs-offline/02-RESEARCH.md
@lib/core/services/cache_service.dart
@lib/core/di/injection.dart

# Phase 2 Requirements
- CLUB-01: User can view list of clubs they belong to
- CLUB-08: Club list works offline (shows cached data)
- OFF-01: App detects network connectivity status
- OFF-02: User can view cached clubs when offline
- OFF-08: Cache respects TTL (5 minutes for matches)

# Research Insights
- Use connectivity_plus for network detection
- Implement TTL cache strategy: store timestamp with data, invalidate after 5 minutes
- Repository pattern: check connectivity, fetch remote if online, fallback to cache
- Hive for local storage with custom adapters for complex objects
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Dependencies and Create Connectivity Service</name>
  <files>
    - pubspec.yaml
    - lib/core/services/connectivity_service.dart
    - lib/core/providers/connectivity_provider.dart
  </files>
  <action>
    1. Add Phase 2 dependencies to pubspec.yaml:
       - connectivity_plus: ^7.0.0 (network state detection)
       - shimmer: ^3.0.0 (loading skeletons)
       - share_plus: ^12.0.1 (invite code sharing)
       Run `flutter pub get`
    
    2. Create lib/core/services/connectivity_service.dart:
       - ConnectivityService class using connectivity_plus
       - Method: Future<bool> get isOnline
       - Stream<bool> get onConnectivityChanged
       - Maps ConnectivityResult.none to offline, all others to online
       - Handle VPN/captive portal edge cases gracefully
    
    3. Create lib/core/providers/connectivity_provider.dart:
       - connectivityServiceProvider: Provider<ConnectivityService>
       - isOnlineProvider: StreamProvider<bool> (real-time connectivity)
       - Auto-dispose on inactivity
    
    4. Update lib/core/di/injection.dart:
       - Export connectivity providers
       - Ensure service lifecycle managed (dispose on app close)
  </action>
  <verify>
    `flutter pub get` completes successfully
    `flutter analyze` shows no errors
    Connectivity service compiles without errors
  </verify>
  <done>
    connectivity_plus, shimmer, share_plus added to pubspec.yaml, ConnectivityService exists with isOnline and onConnectivityChanged, providers configured
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Club and Member Domain Entities</name>
  <files>
    - lib/features/clubs/domain/entities/club.dart
    - lib/features/clubs/domain/entities/member.dart
    - lib/features/clubs/domain/repositories/clubs_repository.dart
  </files>
  <action>
    1. Create lib/features/clubs/domain/entities/club.dart:
       - @freezed class Club with _$Club
       - Fields:
         - id: String
         - name: String
         - logoUrl: String?
         - memberCount: int
         - userRole: ClubRole (OWNER, MANAGER, MEMBER enum)
         - description: String?
         - createdAt: DateTime
       - ClubRole enum with values and display names
       - isAdmin getter (true if OWNER or MANAGER)
    
    2. Create lib/features/clubs/domain/entities/member.dart:
       - @freezed class Member with _$Member
       - Fields:
         - id: String
         - name: String
         - avatarUrl: String?
         - role: ClubRole
         - joinedAt: DateTime
         - stats: MemberStats? (placeholder for Phase 4)
       - @freezed class MemberStats with _$MemberStats
       - Fields: matchesPlayed, goals, assists, rating (basic for Phase 2)
    
    3. Create lib/features/clubs/domain/repositories/clubs_repository.dart:
       - Abstract class ClubsRepository
       - Methods:
         - Future<Result<List<Club>, Failure>> getClubs()
         - Future<Result<Club, Failure>> getClubById(String id)
         - Future<Result<List<Member>, Failure>> getClubMembers(String clubId)
         - Future<Result<String, Failure>> generateInviteCode(String clubId)
       - All methods return Result type from core/utils/result.dart
    
    4. Create directory structure:
       - lib/features/clubs/domain/entities/
       - lib/features/clubs/domain/repositories/
       - lib/features/clubs/domain/usecases/
       - lib/features/clubs/data/models/
       - lib/features/clubs/data/datasources/
       - lib/features/clubs/data/repositories/
  </action>
  <verify>
    `flutter analyze` shows no errors in domain layer
    Entities are pure Dart (no Flutter imports)
    ClubRole enum has all three values
  </verify>
  <done>
    Club and Member entities with freezed, ClubRole enum, ClubsRepository interface, directory structure created
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Data Models and TTL Cache Implementation</name>
  <files>
    - lib/features/clubs/data/models/club_model.dart
    - lib/features/clubs/data/models/member_model.dart
    - lib/core/services/clubs_local_datasource.dart
    - lib/core/services/clubs_remote_datasource.dart
  </files>
  <action>
    1. Create lib/features/clubs/data/models/club_model.dart:
       - ClubModel extends Club entity
       - fromJson/toJson with json_serializable
       - Factory: ClubModel.fromEntity(Club entity)
       - Method: Club toEntity()
       - @HiveType adapter annotations for Hive storage
    
    2. Create lib/features/clubs/data/models/member_model.dart:
       - MemberModel extends Member entity
       - fromJson/toJson with json_serializable
       - MemberStatsModel for nested stats
       - Hive adapter annotations
    
    3. Create lib/core/services/clubs_remote_datasource.dart:
       - ClubsRemoteDataSource interface
       - ClubsRemoteDataSourceImpl using Dio
       - Methods:
         - Future<List<ClubModel>> getClubs()
         - Future<ClubModel> getClubById(String id)
         - Future<List<MemberModel>> getClubMembers(String clubId)
         - Future<String> generateInviteCode(String clubId)
       - API endpoints (placeholders for now):
         - GET /api/clubs
         - GET /api/clubs/{id}
         - GET /api/clubs/{id}/members
         - POST /api/clubs/{id}/invite
    
    4. Create lib/core/services/clubs_local_datasource.dart:
       - ClubsLocalDataSource interface and implementation
       - Hive box: 'clubs_cache'
       - TTL implementation:
         - static const Duration _ttl = Duration(minutes: 5)
         - cacheClubs(List<ClubModel> clubs): stores data + timestamp
         - getCachedClubs(): returns null if expired, else data
         - cacheClubMembers(String clubId, List<MemberModel> members)
         - getCachedClubMembers(String clubId)
         - isCacheValid(String key): checks timestamp against TTL
       - Keys:
         - clubs_cache_key: 'cached_clubs'
         - clubs_cache_timestamp: 'cached_clubs_timestamp'
         - members_cache_key: 'cached_members_{clubId}'
    
    5. Register Hive adapters in lib/main.dart:
       - Hive.registerAdapter(ClubModelAdapter())
       - Hive.registerAdapter(MemberModelAdapter())
       - Hive.registerAdapter(ClubRoleAdapter())
       - Open 'clubs' box on startup
    
    6. Run code generation: `flutter pub run build_runner build --delete-conflicting-outputs`
  </action>
  <verify>
    `flutter pub run build_runner build` completes successfully
    Generated files exist: club_model.g.dart, member_model.g.dart
    Hive adapters registered in main.dart
    TTL cache logic is correct (5 minutes)
  </verify>
  <done>
    ClubModel and MemberModel with Hive adapters, remote and local datasources with TTL cache, code generated, adapters registered
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement Network-Aware Clubs Repository</name>
  <files>
    - lib/features/clubs/data/repositories/clubs_repository_impl.dart
    - lib/core/di/injection.dart
  </files>
  <action>
    1. Create lib/features/clubs/data/repositories/clubs_repository_impl.dart:
       - ClubsRepositoryImpl implements ClubsRepository
       - Dependencies:
         - ClubsRemoteDataSource remote
         - ClubsLocalDataSource local
         - ConnectivityService connectivity
       - getClubs() implementation:
         - Check isOnline
         - If online: fetch from remote, cache to local, return Success(clubs)
         - If offline or error: return cached data if valid, else Failure
         - Never return empty when cache has data
       - getClubById(id) implementation:
         - Check cache first
         - If online and cache stale/missing: fetch remote, cache, return
         - If offline: return cached if available, else Failure
       - getClubMembers(clubId) implementation:
         - Same pattern: online → fetch+cache, offline → fallback to cache
       - generateInviteCode(clubId) implementation:
         - Requires online (admin-only, but check in use case)
         - POST to remote, return code string
         - Offline: return NetworkFailure immediately
       - Error mapping:
         - NetworkException → NetworkFailure
         - CacheException → CacheFailure
         - ServerException → ServerFailure
    
    2. Update lib/core/di/injection.dart:
       - Add clubsRepositoryProvider
       - Add clubsRemoteDataSourceProvider
       - Add clubsLocalDataSourceProvider
       - Wire dependencies: connectivity -> local/remote -> repository
       - Export all clubs providers
    
    3. Pattern requirements:
       - Cache-first for reads
       - Network-aware with graceful degradation
       - TTL respected (handled in local datasource)
       - Result type for all returns
  </action>
  <verify>
    `flutter analyze` shows no errors
    Repository implements all interface methods
    Connectivity check integrated correctly
    Provider dependencies wired properly
  </verify>
  <done>
    ClubsRepositoryImpl with network-aware logic, cache-first strategy, proper error handling, all providers configured
  </done>
</task>

</tasks>

<verification>
1. **Connectivity Detection**: Toggle WiFi/airplane mode, app should detect changes
2. **TTL Cache**: Wait 5+ minutes, cache should invalidate automatically
3. **Offline Fallback**: Disable network, app should show cached clubs
4. **Repository Pattern**: All CRUD operations go through repository
5. **Code Generation**: All .g.dart files generated and committed
6. **Type Safety**: No dynamic types, all models use freezed/json_serializable
</verification>

<success_criteria>
1. **Network Detection** (OFF-01):
   - ConnectivityService detects online/offline state
   - Stream updates in real-time
   - Works on both mobile and web platforms

2. **Cache Infrastructure** (CLUB-08, OFF-02, OFF-08):
   - Clubs data cached in Hive with 5-minute TTL
   - TTL automatically invalidates stale data
   - Repository returns cached data when offline
   - No empty states due to network issues

3. **Data Models**:
   - Club entity: id, name, logoUrl, memberCount, userRole
   - Member entity: id, name, avatarUrl, role, stats
   - ClubRole enum: OWNER, MANAGER, MEMBER
   - Models extend entities with JSON serialization

4. **Repository Implementation** (CLUB-01):
   - getClubs() returns cached or fresh data
   - Cache-first strategy for optimal UX
   - Graceful offline fallback
   - Result type for error handling

5. **Dependency Injection**:
   - All services and repositories available via Riverpod
   - Proper lifecycle management
   - Testable abstractions
</success_criteria>

<output>
After completion, create `.planning/phases/02-clubs-offline/02-01-SUMMARY.md`

Summary should include:
- Connectivity service API
- TTL cache implementation details
- Repository pattern with network awareness
- Data models structure
- Provider configuration
- Next plan dependencies (Plan 02 needs providers and entities)
</output>
