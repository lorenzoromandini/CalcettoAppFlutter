---
phase: 02-clubs-offline
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - lib/features/clubs/presentation/providers/clubs_list_provider.dart
  - lib/features/clubs/presentation/providers/active_club_provider.dart
  - lib/features/clubs/presentation/screens/clubs_list_screen.dart
  - lib/features/clubs/presentation/widgets/club_list_item.dart
  - lib/features/clubs/presentation/widgets/clubs_list_skeleton.dart
  - lib/features/home/presentation/screens/clubs_screen.dart
autonomous: true
must_haves:
  truths:
    - "User sees list of clubs they belong to"
    - "Active club has star icon indicator"
    - "Loading shows shimmer skeleton, not spinner"
    - "Pull-to-refresh updates the list"
    - "List displays club image, name, member count, role badge"
  artifacts:
    - path: "lib/features/clubs/presentation/screens/clubs_list_screen.dart"
      provides: "Clubs list screen with pull-to-refresh and loading states"
      min_lines: 100
      contains: ["RefreshIndicator", "ConsumerWidget", "AsyncValue"]
    - path: "lib/features/clubs/presentation/widgets/club_list_item.dart"
      provides: "Individual club list item with star indicator"
      min_lines: 60
      contains: ["ListTile", "Icon(Icons.star)", "role badge"]
    - path: "lib/features/clubs/presentation/widgets/clubs_list_skeleton.dart"
      provides: "Shimmer loading skeleton for clubs list"
      contains: ["Shimmer.fromColors", "skeleton placeholders"]
    - path: "lib/features/clubs/presentation/providers/clubs_list_provider.dart"
      provides: "Riverpod provider for clubs list with refresh"
      exports: ["clubsListProvider"]
    - path: "lib/features/clubs/presentation/providers/active_club_provider.dart"
      provides: "Active club state management with persistence"
      exports: ["activeClubProvider"]
  key_links:
    - from: "lib/features/clubs/presentation/providers/clubs_list_provider.dart"
      to: "lib/features/clubs/data/repositories/clubs_repository_impl.dart"
      via: "Repository injection"
      pattern: "ref.read(clubsRepositoryProvider)"
    - from: "lib/features/clubs/presentation/screens/clubs_list_screen.dart"
      to: "lib/features/clubs/presentation/providers/clubs_list_provider.dart"
      via: "Provider watch"
      pattern: "ref.watch(clubsListProvider)"
    - from: "lib/features/clubs/presentation/widgets/club_list_item.dart"
      to: "lib/features/clubs/presentation/providers/active_club_provider.dart"
      via: "Active club comparison"
      pattern: "club.id == activeClubId"
---

<objective>
Create clubs list UI with loading skeletons, pull-to-refresh, and active club indicator. Implement list layout with club image, name, member count, and role badges per user specifications.

Purpose: Provide the primary interface for viewing and managing club memberships. The list must be responsive, show loading states with shimmer effects, and clearly indicate the currently active club with a star icon.

Output: Complete clubs list screen with all UI components, providers for state management, and navigation to club details.
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-clubs-offline/02-01-SUMMARY.md
@.planning/phases/02-clubs-offline/02-CONTEXT.md

# Phase 2 Requirements
- CLUB-01: User can view list of clubs they belong to
- CLUB-02: User can switch between clubs easily
- CLUB-06: User can see their role in each club (OWNER/MANAGER/MEMBER)
- CLUB-09: App shows loading skeleton while fetching clubs
- UI-06: App supports pull-to-refresh on list screens
- PERF-04: List scrolling is smooth (no jank)

# User Locked Decisions (from CONTEXT.md)
**Club List Layout:**
- Display style: List items (not cards or grid)
- Per club information: Club image (optional), name, member count, role badge
- Active indicator: Star icon on currently selected club
- Navigation: Tap club → opens club detail screen with tabs
- Create button: In app bar (NOT floating action button)

**Claude's Discretion:**
- Club switching mechanism and UI placement
- Loading skeleton/shimmer design
- Pull-to-refresh implementation details
- Empty state design for new users
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Clubs List Provider and Active Club State</name>
  <files>
    - lib/features/clubs/presentation/providers/clubs_list_provider.dart
    - lib/features/clubs/presentation/providers/active_club_provider.dart
    - lib/features/clubs/clubs_feature.dart
  </files>
  <action>
    1. Create lib/features/clubs/presentation/providers/clubs_list_provider.dart:
       - @riverpod class ClubsList extends _$ClubsList
       - build() returns Future<List<Club>>
       - Uses clubsRepositoryProvider to fetch clubs
       - Handles Result type with fold pattern
       - refresh() method for pull-to-refresh:
         - state = const AsyncValue.loading()
         - state = await AsyncValue.guard(() => fetch with forceRefresh)
       - forceRefresh parameter to bypass cache
       - Error handling: throws failures for UI to catch
       - Offline handling: returns cached data automatically via repository
    
    2. Create lib/features/clubs/presentation/providers/active_club_provider.dart:
       - @riverpod class ActiveClub extends _$ActiveClub
       - Static const String _activeClubIdKey = 'active_club_id'
       - build() returns Future<Club?>:
         - Load cached ID from Hive
         - If cached ID exists: fetch club by ID from repository
         - If no cached ID or fetch fails: return null
       - setActiveClub(String clubId) method:
         - Save clubId to Hive cache
         - Update state with AsyncValue.loading()
         - Fetch full club data via repository
         - Update state with result
         - Reset navigation to home tab (ref.read(navigationProvider.notifier).setTab(0))
       - clearActiveClub() method:
         - Delete from Hive
         - Set state to AsyncData(null)
       - isActive(Club club) helper method
    
    3. Create lib/features/clubs/clubs_feature.dart:
       - Export all public APIs for clubs feature
       - Export domain/entities/
       - Export presentation/screens/
       - Export presentation/providers/
       - Export presentation/widgets/
       - Clean public interface for other features
  </action>
  <verify>
    `flutter pub run build_runner build` for code generation
    `flutter analyze` shows no errors
    Providers have correct async signatures
  </verify>
  <done>
    ClubsList provider with refresh capability, ActiveClub provider with persistence, feature exports, code generated
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Loading Skeleton and Club List Item Widgets</name>
  <files>
    - lib/features/clubs/presentation/widgets/clubs_list_skeleton.dart
    - lib/features/clubs/presentation/widgets/club_list_item.dart
    - lib/features/clubs/presentation/widgets/role_badge.dart
  </files>
  <action>
    1. Create lib/features/clubs/presentation/widgets/clubs_list_skeleton.dart:
       - ClubsListSkeleton StatelessWidget
       - Uses Shimmer.fromColors (shimmer package)
       - Base color: Colors.grey.shade300
       - Highlight color: Colors.grey.shade100
       - Shows 6 placeholder items (matches typical screen height)
       - Each placeholder:
         - Circle (48x48) for club image
         - Two lines of text: full width title, short subtitle
         - Material 3 list item spacing (16px horizontal, 12px vertical)
         - Row layout matching actual list item
       - Keep simple to avoid jank (no complex animations)
       - Use RepaintBoundary if needed for performance
    
    2. Create lib/features/clubs/presentation/widgets/club_list_item.dart:
       - ClubListItem StatelessWidget
       - Props:
         - Club club
         - bool isActive
         - VoidCallback onTap
         - VoidCallback? onLongPress (for switching)
       - Layout (ListTile-based):
         - Leading: Club logo (CircleAvatar with NetworkImage or placeholder)
         - Title: Club name (bodyLarge, max 1 line, ellipsis)
         - Subtitle: Member count (bodySmall: "X members")
         - Trailing:
           - Role badge (Chip with role name)
           - If isActive: Star icon (Icons.star, color: Colors.amber)
       - Visual design:
         - Material 3 list item style
         - Proper padding and spacing
         - Ripple effect on tap
         - Optional: slight background tint when active (surfaceContainerHighest)
       - Handle missing logo gracefully (placeholder icon)
    
    3. Create lib/features/clubs/presentation/widgets/role_badge.dart:
       - RoleBadge StatelessWidget
       - Props: ClubRole role
       - Material 3 Chip style
       - Colors:
         - OWNER: Gold/amber chip
         - MANAGER: Blue chip
         - MEMBER: Grey/neutral chip
       - Compact size for list items
       - Text: role name (OWNER, MANAGER, MEMBER)
  </action>
  <verify>
    Widgets compile without errors
    Shimmer widget imports correctly from shimmer package
    ClubListItem matches design requirements (star for active, role badge)
  </verify>
  <done>
    ClubsListSkeleton with shimmer, ClubListItem with star/active indicator, RoleBadge widget
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Clubs List Screen with Pull-to-Refresh</name>
  <files>
    - lib/features/clubs/presentation/screens/clubs_list_screen.dart
    - lib/features/home/presentation/screens/clubs_screen.dart
  </files>
  <action>
    1. Create lib/features/clubs/presentation/screens/clubs_list_screen.dart:
       - ClubsListScreen ConsumerWidget
       - Scaffold with Material 3 AppBar:
         - Title: "My Clubs"
         - Actions: Create club button (IconButton with Icons.add)
           - Only if user can create clubs (future feature)
           - For now: placeholder that shows "Coming soon" snackbar
       - Body: Consumer builder watching clubsListProvider
       - AsyncValue states:
         - loading: ClubsListSkeleton widget
         - error: Error widget with retry button
         - data: RefreshIndicator wrapping ListView.builder
       - RefreshIndicator:
         - onRefresh: () => ref.read(clubsListProvider.notifier).refresh()
         - Material 3 styling (color matches theme)
       - ListView.builder:
         - itemCount: clubs.length
         - itemBuilder: (context, index) => ClubListItem
         - physics: AlwaysScrollableScrollPhysics (for pull-to-refresh even with few items)
         - cacheExtent: 200 (for smooth scrolling)
       - ClubListItem configuration:
         - club: clubs[index]
         - isActive: activeClub?.id == club.id (watch activeClubProvider)
         - onTap: () => Navigator.push to ClubDetailScreen
         - onLongPress: () => show switch confirmation dialog (for CLUB-02)
       - Empty state:
         - If clubs.isEmpty: show illustration + "No clubs yet" + "Join a club or create one"
       - Offline indicator integration:
         - Watch isOnlineProvider
         - Show MaterialBanner at top when offline (with cached data message)
       - Performance optimizations:
         - const constructors where possible
         - RepaintBoundary around list items
         - Avoid setState in list items
    
    2. Update lib/features/home/presentation/screens/clubs_screen.dart:
       - Currently placeholder, replace with ClubsListScreen
       - Or keep as wrapper that sets up proper providers
       - Ensure proper route setup
       - Integrate with existing MainLayout navigation
    
    3. Update navigation/routing:
       - Ensure Clubs tab in bottom nav navigates to clubs list
       - Deep linking support for /clubs route (future)
  </action>
  <verify>
    Screen compiles without errors
    Pull-to-refresh gesture works
    List scrolls smoothly (60fps on device)
    Star icon appears on active club
    Create button visible in app bar
  </verify>
  <done>
    ClubsListScreen with pull-to-refresh, loading skeletons, active club indicator, create button in app bar
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement Club Switching Mechanism</name>
  <files>
    - lib/features/clubs/presentation/widgets/club_switcher.dart
    - lib/core/widgets/app_drawer.dart
  </files>
  <action>
    1. Create lib/features/clubs/presentation/widgets/club_switcher.dart:
       - ClubSwitcher StatelessWidget
       - Shows current active club info
       - Tap to open club selection bottom sheet/dialog
       - Props: compact (for app bar) or expanded (for drawer)
    
    2. Create club switch bottom sheet:
       - ClubSwitcherSheet ConsumerWidget
       - Shows list of all clubs with radio buttons
       - Current club marked with checkmark
       - Tap to select, auto-dismiss on selection
       - Shows club image, name, role
       - Loading state if switching in progress
       - "Switch" button confirmation (optional)
    
    3. Update lib/core/widgets/app_drawer.dart:
       - Add club switcher section at top
       - Show active club name and role
       - Tap to open switcher sheet
       - Quick switch dropdown or sheet
       - If no active club: "Select a club" prompt
    
    4. Alternative: Long-press on club list item
       - Show confirmation dialog: "Switch to [Club Name]?"
       - On confirm: call ref.read(activeClubProvider.notifier).setActiveClub(club.id)
       - Show success snackbar: "Switched to [Club Name]"
       - Auto-navigate to Home tab (handled by provider)
    
    5. Implement switch behavior:
       - Reset to Home tab (navigationProvider.setTab(0))
       - Clear any club-specific cached data if needed
       - Update all club-dependent providers via ref.invalidate
  </action>
  <verify>
    Club switcher shows current active club
    Switching updates active club provider
    Navigation resets to home tab
    All club-dependent providers refresh
  </verify>
  <done>
    Club switching UI in drawer and list, bottom sheet for selection, navigation reset on switch
  </done>
</task>

</tasks>

<verification>
1. **Visual Verification**: Clubs list shows with club image, name, member count, role badge
2. **Active Club**: Star icon visible on currently selected club
3. **Loading State**: Shimmer skeleton appears during initial load (not spinner)
4. **Pull-to-Refresh**: Pulling down triggers refresh with spinner
5. **Offline State**: Banner appears when offline, cached data shown
6. **Scrolling**: Smooth 60fps scroll on mid-range device
7. **Club Switching**: Long-press or switcher UI changes active club
8. **Navigation Reset**: After switch, app shows Home tab with new club context
9. **Create Button**: Visible in app bar (not FAB)
</verification>

<success_criteria>
1. **Club List Display** (CLUB-01, CLUB-06):
   - List items show club image (placeholder if missing)
   - Club name displayed clearly
   - Member count shown (e.g., "24 members")
   - Role badge visible (OWNER/MANAGER/MEMBER)
   - Active club marked with star icon

2. **Loading Experience** (CLUB-09):
   - Shimmer skeleton during initial load
   - Skeleton matches list item layout
   - No jank or stuttering during animation
   - Real data replaces skeleton smoothly

3. **Pull-to-Refresh** (UI-06):
   - Standard Material 3 pull gesture works
   - RefreshIndicator shows progress
   - Data updates after refresh completes
   - Works in both online and offline modes

4. **Club Switching** (CLUB-02):
   - Multiple ways to switch (drawer, long-press)
   - Clear indication of current active club
   - Confirmation prompt before switching
   - Resets to Home tab automatically
   - All club-dependent data refreshes

5. **Performance** (PERF-04):
   - List scrolls at 60fps
   - No dropped frames during scroll
   - Efficient widget rebuilds
   - Proper use of const and RepaintBoundary

6. **Layout Compliance**:
   - List items (not cards or grid) per user decision
   - Create button in app bar (not FAB)
   - Active indicator is star icon
   - Navigation to detail on tap
</success_criteria>

<output>
After completion, create `.planning/phases/02-clubs-offline/02-02-SUMMARY.md`

Summary should include:
- Clubs list screen structure
- Provider architecture (clubsList, activeClub)
- Loading skeleton design
- Club switching mechanism
- Performance optimizations applied
- Next plan dependencies (Plan 03 needs club detail screen)
</output>
