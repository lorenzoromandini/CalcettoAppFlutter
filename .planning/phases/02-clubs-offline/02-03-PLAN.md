---
phase: 02-clubs-offline
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - lib/features/clubs/presentation/screens/club_detail_screen.dart
  - lib/features/clubs/presentation/widgets/member_card.dart
  - lib/features/clubs/presentation/screens/club_members_screen.dart
  - lib/features/clubs/presentation/widgets/invite_code_generator.dart
  - lib/features/clubs/presentation/providers/club_members_provider.dart
  - lib/features/clubs/presentation/providers/invite_code_provider.dart
autonomous: true
must_haves:
  truths:
    - "Club detail screen shows name, logo, member count"
    - "Members displayed in FIFA-style player cards"
    - "Role indicators shown as icons (not badges/text)"
    - "Admin users can generate and share invite codes"
    - "Invite codes are one-time use only"
  artifacts:
    - path: "lib/features/clubs/presentation/screens/club_detail_screen.dart"
      provides: "Club detail with tabs (info, members, matches)"
      min_lines: 150
      contains: ["TabBar", "ClubInfoTab", "ClubMembersTab"]
    - path: "lib/features/clubs/presentation/widgets/member_card.dart"
      provides: "FIFA-style player card for member display"
      min_lines: 100
      contains: ["gradient background", "role icon", "stats display"]
    - path: "lib/features/clubs/presentation/widgets/invite_code_generator.dart"
      provides: "Invite code generation and sharing UI"
      contains: ["generate button", "Share.share()", "8-char code display"]
    - path: "lib/features/clubs/presentation/screens/club_members_screen.dart"
      provides: "Grid/list of member cards"
      contains: ["GridView", "MemberCard", "sorting/grouping"]
  key_links:
    - from: "lib/features/clubs/presentation/screens/club_detail_screen.dart"
      to: "lib/features/clubs/presentation/providers/club_members_provider.dart"
      via: "Members tab fetches members"
      pattern: "ref.watch(clubMembersProvider(clubId))"
    - from: "lib/features/clubs/presentation/widgets/invite_code_generator.dart"
      to: "lib/features/clubs/data/repositories/clubs_repository_impl.dart"
      via: "Generate invite code"
      pattern: "repository.generateInviteCode()"
    - from: "lib/features/clubs/presentation/widgets/member_card.dart"
      to: "lib/features/clubs/domain/entities/member.dart"
      via: "Display member data"
      pattern: "Member entity fields"
---

<objective>
Create club detail screen with tabs, FIFA-style member cards, role icons, and invite code generation for admins.

Purpose: Complete the club management experience with detailed club views, visually appealing member cards that foreshadow Phase 4 FIFA cards, and admin-only invite code sharing functionality.

Output: Club detail screen with info/members tabs, FIFA-style member cards, invite code generator with sharing, role-based permissions.
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-clubs-offline/02-01-SUMMARY.md
@.planning/phases/02-clubs-offline/02-CONTEXT.md

# Phase 2 Requirements
- CLUB-03: User can view club details (name, logo, member count)
- CLUB-04: User can view club members with their roles
- CLUB-05: User can generate and share invite codes for their club
- CLUB-06: User can see their role in each club (OWNER/MANAGER/MEMBER)
- CLUB-07: User can view club statistics summary

# User Locked Decisions
**Club Detail Navigation:**
- Tap club → opens club detail screen with tabs
- Tabs: Info, Members, Matches (matches placeholder for Phase 3)

**Member Display:**
- Presentation style: FIFA-style player cards (foreshadows Phase 4)
- Information shown: All info on the card (name, stats, etc.)
- Role indicators: Icons (not color badges or text)
- Interactivity: Tappable to view full member profile and statistics

**Invite Code Sharing:**
- Generation permission: Admin only (Owners and Managers)
- Security requirement: One-time use only
- Expiration: Never expire
- Sharing mechanism: Claude's discretion
- Code format: Claude's discretion (use 8-char alphanumeric)

**Claude's Discretion:**
- Invite code format and sharing UX
- Member list sorting and grouping
- Empty state design
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Club Detail Screen with Tabs</name>
  <files>
    - lib/features/clubs/presentation/screens/club_detail_screen.dart
    - lib/features/clubs/presentation/widgets/club_info_tab.dart
  </files>
  <action>
    1. Create lib/features/clubs/presentation/screens/club_detail_screen.dart:
       - ClubDetailScreen ConsumerWidget
       - Route parameter: Club club (passed from list)
       - DefaultTabController with 3 tabs:
         - Info (club details and stats)
         - Members (FIFA-style cards)
         - Matches (placeholder for Phase 3)
       - AppBar:
         - Title: club.name
         - Actions:
           - Share button (if admin, generates invite code)
           - More options menu (future: edit, leave club)
       - TabBar with Material 3 styling:
         - Icons + labels: info, people, sports
         - Animated indicator
       - TabBarView with 3 tabs
       - Pull-to-refresh on each tab
       - Offline indicator if needed
    
    2. Create lib/features/clubs/presentation/widgets/club_info_tab.dart:
       - ClubInfoTab StatelessWidget
       - Props: Club club
       - Layout:
         - Club logo (large, centered, 120x120)
         - Club name (headlineMedium, centered)
         - Member count chip (bodyLarge)
         - Description section (if available)
         - Statistics summary section (CLUB-07):
           - Total matches played
           - Win rate (placeholder)
           - Average attendance (placeholder)
           - Cards in grid layout (2 columns)
         - Your role section:
           - Large role icon
           - Role name text
           - Permissions list (admin features)
         - Club creation date
       - Material 3 card styling for sections
       - Scrollable SingleChildScrollView
  </action>
  <verify>
    Tab navigation works smoothly
    Club data displays correctly
    All tabs accessible
    Pull-to-refresh works
  </verify>
  <done>
    Club detail screen with 3 tabs, info tab with club details and stats, proper navigation
  </done>
</task>

<task type="auto">
  <name>Task 2: Create FIFA-Style Member Card Widget</name>
  <files>
    - lib/features/clubs/presentation/widgets/member_card.dart
    - lib/features/clubs/presentation/widgets/role_icon.dart
  </files>
  <action>
    1. Create lib/features/clubs/presentation/widgets/member_card.dart:
       - MemberCard StatelessWidget (FIFA-style)
       - Props: Member member, VoidCallback? onTap
       - Layout (aspect ratio ~0.7 for card feel):
         - Container with gradient background:
           - OWNER: Gold/amber gradient (Colors.amber[200] to Colors.amber[600])
           - MANAGER: Blue gradient (Colors.blue[200] to Colors.blue[600])
           - MEMBER: Silver/grey gradient (Colors.grey[300] to Colors.grey[600])
         - ClipRRect with border radius 12
         - Stack layout:
           - Background pattern (optional CustomPainter for texture)
           - Border with role-appropriate color
       - Content structure:
         - Top section (30%):
           - Row with:
             - Avatar (CircleAvatar, 40px, top-left)
             - Spacer
             - RoleIcon (top-right, 24px)
         - Middle section (40%):
           - Member name (titleMedium, white text, max 2 lines)
           - Joined date (bodySmall, semi-transparent white)
         - Bottom section (30%):
           - Stats preview (Phase 4 foreshadow):
             - Row with 3 mini-stat items:
               - Matches: icon + count
               - Goals: icon + count
               - Rating: number with star icon
             - If no stats yet: show "New member" or placeholder
       - Visual polish:
         - BoxShadow for depth (elevation 4)
         - Text shadows for readability
         - Gradient overlay for text legibility
         - Subtle border highlight
       - Interactivity:
         - InkWell or GestureDetector on whole card
         - onTap callback for navigation to profile
         - Scale animation on tap (0.98)
       - Performance:
         - const where possible
         - Avoid heavy shadows on low-end
         - Image caching for avatars
    
    2. Create lib/features/clubs/presentation/widgets/role_icon.dart:
       - RoleIcon StatelessWidget
       - Props: ClubRole role, double size
       - Returns Icon based on role:
         - OWNER: Icons.stars or Icons.workspace_premium (gold star)
         - MANAGER: Icons.shield or Icons.admin_panel_settings (blue shield)
         - MEMBER: Icons.person or Icons.people (grey person)
       - Consistent sizing and colors matching card gradients
       - Tooltip with role name on long-press
  </action>
  <verify>
    Member card renders with proper styling
    Role icons show correct icons for each role
    Gradient colors match role hierarchy
    Card is tappable with proper feedback
  </verify>
  <done>
    FIFA-style MemberCard with role-based gradients, RoleIcon widget, proper styling and interactivity
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Club Members Screen with Grid Layout</name>
  <files>
    - lib/features/clubs/presentation/screens/club_members_screen.dart
    - lib/features/clubs/presentation/providers/club_members_provider.dart
    - lib/features/clubs/presentation/widgets/members_grid_skeleton.dart
  </files>
  <action>
    1. Create lib/features/clubs/presentation/providers/club_members_provider.dart:
       - @riverpod class ClubMembers extends _$ClubMembers
       - Family provider: clubMembersProvider(String clubId)
       - build(clubId) returns Future<List<Member>>
       - Uses clubsRepositoryProvider
       - refresh() method for pull-to-refresh
       - Sorting:
         - Default: By role (OWNER first, then MANAGER, then MEMBER)
         - Secondary: By name alphabetical
         - Tertiary: By join date (newest first within same role)
       - Error handling with AsyncValue
    
    2. Create lib/features/clubs/presentation/screens/club_members_screen.dart:
       - ClubMembersTab ConsumerWidget (used in tab view)
       - Props: String clubId
       - Watches clubMembersProvider(clubId)
       - AsyncValue states:
         - loading: MembersGridSkeleton
         - error: Error widget with retry
         - data: RefreshIndicator with GridView
       - GridView.builder:
         - SliverGridDelegateWithFixedCrossAxisCount:
           - crossAxisCount: 2 (on mobile)
           - childAspectRatio: 0.7 (card shape)
           - crossAxisSpacing: 12
           - mainAxisSpacing: 12
         - Padding: 16 all sides
         - itemCount: members.length
         - itemBuilder: MemberCard
       - Sorting/grouping options (Claude's discretion):
         - Segmented button: "All | Owners | Managers | Members"
         - Or: Group headers in list (if using list instead of grid)
       - Empty state:
         - "No members yet" (rare, but handle)
         - "Be the first to join!"
       - Pull-to-refresh:
         - RefreshIndicator wrapping GridView
         - Triggers provider.refresh()
       - Performance:
         - GridView.builder for lazy loading
         - MemberCard is const when possible
         - Image cache for avatars
    
    3. Create lib/features/clubs/presentation/widgets/members_grid_skeleton.dart:
       - MembersGridSkeleton StatelessWidget
       - Shimmer effect (reuses shimmer package)
       - Shows 8 placeholder cards in grid (2 columns)
       - Each placeholder:
         - Rectangular card shape (aspect ratio 0.7)
         - Circle for avatar
         - Lines for name and stats
       - Matches actual grid layout exactly
  </action>
  <verify>
    Members load and display in grid
    Grid scrolls smoothly at 60fps
    Sorting works (owners first)
    Pull-to-refresh works
    Skeleton shows during loading
  </verify>
  <done>
    Club members screen with grid of FIFA-style cards, provider with sorting, skeleton loading
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement Invite Code Generation and Sharing</name>
  <files>
    - lib/features/clubs/presentation/widgets/invite_code_generator.dart
    - lib/features/clubs/presentation/providers/invite_code_provider.dart
  </files>
  <action>
    1. Create lib/features/clubs/presentation/providers/invite_code_provider.dart:
       - @riverpod class InviteCode extends _$InviteCode
       - State: AsyncValue<String?> (null = not generated)
       - generate(String clubId) method:
         - Check if user is admin (OWNER or MANAGER)
         - If not admin: set error state with permission message
         - If admin: call repository.generateInviteCode(clubId)
         - On success: update state with code
         - On error: set error state
       - clear() method to reset state
       - Auto-clear after 5 minutes (optional security)
    
    2. Create lib/features/clubs/presentation/widgets/invite_code_generator.dart:
       - InviteCodeGenerator ConsumerWidget
       - Props: String clubId, ClubRole userRole
       - Admin-only display:
         - If not admin: show "Contact an admin to invite members"
         - If admin: show generation UI
       - States:
         - Not generated: "Generate Invite Code" button
         - Loading: CircularProgressIndicator
         - Generated: Display code with share option
         - Error: Error message with retry
       - Generate UI:
         - FilledButton.tonal: "Generate Invite Code"
         - Icon: Icons.add_link or Icons.person_add
       - Generated UI:
         - Code display in monospace font, large (24pt)
         - Copy button (Icons.copy)
         - Share button (Icons.share)
         - One-time use warning text (bodySmall, secondary color)
         - "Expires: Never" text
       - Code format: 8-character alphanumeric uppercase (e.g., "AB3D-9F2K")
       - Sharing implementation:
         - Share.share() from share_plus package
         - ShareParams with:
           - text: "Join my football club! Use invite code: $code"
           - subject: "Club Invitation"
           - sharePositionOrigin for iPad support
         - BuildContext extension for RenderBox
       - Copy to clipboard:
         - Clipboard.setData(ClipboardData(text: code))
         - Show snackbar: "Code copied to clipboard"
       - Security notes UI:
         - Small text: "This code can only be used once"
         - "Share privately with trusted people"
    
    3. Integration:
       - Add InviteCodeGenerator to club detail screen (Info tab)
       - Or accessible via share button in AppBar
       - Ensure proper context for share dialog positioning
    
    4. Permission handling:
       - Check userRole from club context
       - Disable/hide for MEMBER role
       - Show helpful message for non-admins
  </action>
  <verify>
    Admin users can generate codes
    Codes are 8-char alphanumeric
    Share dialog opens with proper message
    Copy to clipboard works
    Non-admins see permission message
  </verify>
  <done>
    Invite code generation with admin-only permission, 8-char format, share_plus integration, copy to clipboard
  </done>
</task>

</tasks>

<verification>
1. **Club Detail Tabs**: Info, Members, Matches tabs all accessible
2. **Club Info**: Shows logo, name, member count, stats summary
3. **Member Cards**: FIFA-style cards with gradients, role icons, stats
4. **Role Icons**: OWNER=star, MANAGER=shield, MEMBER=person icons
5. **Invite Generation**: Admin-only, 8-char code, shareable
6. **One-Time Warning**: UI clearly states code is single-use
7. **Share Functionality**: Native share sheet opens with pre-filled text
8. **Grid Layout**: 2-column grid, proper spacing, scrolls smoothly
9. **Pull-to-Refresh**: Works on members tab
10. **Sorting**: Owners appear first, then managers, then members
</verification>

<success_criteria>
1. **Club Details** (CLUB-03):
   - Name and logo displayed prominently
   - Member count visible
   - Statistics summary section present
   - Description shown if available

2. **Member Display** (CLUB-04, CLUB-06):
   - FIFA-style player cards with role-based gradients
   - Role icons (not badges) for visual distinction
   - Member name and stats on card
   - Cards are tappable for full profile
   - Grid layout for visual appeal

3. **Role Indicators** (user requirement):
   - Icons only (not color badges or text)
   - OWNER: Star/crown icon (gold)
   - MANAGER: Shield icon (blue)
   - MEMBER: Person icon (grey)
   - Consistent across all member displays

4. **Invite Codes** (CLUB-05):
   - Admin-only generation (Owners and Managers)
   - 8-character alphanumeric format
   - One-time use enforced by backend
   - Never expires per requirements
   - Native share dialog with pre-filled message
   - Copy to clipboard option

5. **Tab Navigation**:
   - Info tab: Club details and stats
   - Members tab: FIFA-style cards
   - Matches tab: Placeholder for Phase 3
   - Smooth tab switching
   - Independent scroll and refresh per tab

6. **Visual Design**:
   - FIFA-style cards foreshadow Phase 4
   - Gradient backgrounds based on role
   - Material 3 styling throughout
   - Professional, polished appearance
</success_criteria>

<output>
After completion, create `.planning/phases/02-clubs-offline/02-03-SUMMARY.md`

Summary should include:
- Club detail screen structure with tabs
- FIFA-style member card implementation
- Role icon system
- Invite code generation flow
- Member sorting and grouping
- Admin permission handling
- Next plan dependencies (Plan 04 needs offline indicator)
</output>
